# nostr_automerge library

This alpha crate validates bounded raw NIP-01 evidence, exposes semantic IDs,
provides pure deterministic authoring, and verifies optional full-history
checkpoints. It owns no keys, clocks, storage, network clients, async runtime,
application schema, or publication policy. Start with the repository
`README.md` and the runnable `basic_authoring` example.
