semantic_id!(
    HistoryDigest,
    "The protocol history digest defined by the v1 binary contract."
);
semantic_id!(
    DispositionsDigest,
    "The protocol disposition digest defined by the v1 binary contract."
);
