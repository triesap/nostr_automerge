semantic_id!(
    ChangeHash,
    "The SHA-256 identity of one canonical Automerge change."
);
semantic_id!(
    SnapshotHash,
    "The SHA-256 identity of complete raw checkpoint bytes."
);
semantic_id!(
    ChunkHash,
    "The SHA-256 identity of one raw checkpoint chunk."
);
