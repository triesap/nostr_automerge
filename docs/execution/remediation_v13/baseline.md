# Causal projection follow-up baseline

The reviewed public predecessor is
`00ef954ff2dece37119ad235638046ffaa7305d4`. The completed remediation-v12
sequence, distribution-v13, signed Event identities, canonical ample-work
reports, and all historical evidence are immutable.

The active sequence is RCLD 116 through RCLD 120 and `step_1420` through
`step_1452`. It owns Findings 104 through 112. Finding 080 and the external
assurance, event-kind allocation, NIP submission, production qualification,
publication, release, and remote-mutation holds remain held.

The initial defect set is resource-accounting and evidence-local. It does not
authorize a protocol, wire, Event, release, or remote-action change.
