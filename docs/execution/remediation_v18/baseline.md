# V18 sealed-boundary baseline

The reviewed public baseline is commit
`8673ff8546b9e9d57218c15a4b81890d82137184` with tree
`f6b6d044553d06e71d7c48f4a30d41922e99a2f0`.

Executable reproduction is intentionally evaluated against that immutable Git
object rather than the moving worktree. It confirms four pre-charge operation
observer calls, fixed-label proof assertions, helper-wide direct mutations, and
in-process-only property evaluation in the v17 mutation runner. Later runtime
or assurance repairs therefore do not erase the original evidence.

The NIP, requirements registry, report contract, signed fixtures, distribution,
and ample-work output are frozen by `spec/remediation_v18_authority.json`.
