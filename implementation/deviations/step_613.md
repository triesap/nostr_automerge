# RCLD 35 Combined Green Slice

Steps `step_613` through `step_620` are implemented as one reviewable
specification-and-vector slice. The companion specification, Automerge profile,
stable requirements registry, neutral causal-counter vector, executable Rust
anchor, import provenance, and fail-closed validators form one authority graph;
committing an intermediate subset would leave the specification gate stale.

The externally authored `spec/NIP_DRAFT.md` remains byte-for-byte unchanged at
SHA-256
`67019c8ea680714052c65226f620a8e1a60b9b10a8f158603063a835a7bbc7a3`.
The repository therefore makes no claim that the NIP prose was edited,
reconciled, submitted, or adopted. It states the required causal-counter,
selected-manifest, and dynamic event-disposition semantics in
implementation-owned companion authority instead.

All 87 normative requirement identifiers retain their exact order. Six
existing requirements receive clarified text and repository-owned authority;
no identifier is added, removed, or renumbered. The signed distribution remains
assigned to RCLD 36. Phase closure and refreshed commit-bound evidence remain
assigned to `step_621`.
