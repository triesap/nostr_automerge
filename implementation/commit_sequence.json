{
  "schema": "nostr_automerge.commit_sequence.v1",
  "project": "nostr_automerge_v1_spec",
  "step_count": 192,
  "steps": [
    {
      "sequence": 1,
      "step_id": "step_001",
      "phase": "phase_00_spec_and_repository_control",
      "phase_step": 1,
      "title": "Import the approved handoff baseline",
      "purpose": "Make the approved durable contracts visible inside the target repository before implementation starts.",
      "scope": "Copy the authoritative NIP draft, companion specification, requirements registry, ADRs, and handoff checksum into repository-owned `spec/` and `docs/` paths. Record the source package SHA-256 and import date. Do not edit protocol content in this commit.",
      "files": "spec/NIP_DRAFT.md; spec/NOSTR_AUTOMERGE_V1_SPEC.md; spec/requirements.json; docs/handoff_provenance.md",
      "tests": "Add a script/test that checks imported files exist and provenance fields are non-empty.",
      "commit": "docs(spec): import approved nostr_automerge_v1_spec baseline",
      "extra": [],
      "verification_commands": [
        "python3 scripts/validate_spec.py  # once introduced; otherwise run the strongest existing schema/checksum validation",
        "git diff --check",
        "# Discover and run any repository documentation/link/schema validation command introduced by this step."
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 2,
      "step_id": "step_002",
      "phase": "phase_00_spec_and_repository_control",
      "phase_step": 2,
      "title": "Add repository-local agent instructions",
      "purpose": "Keep coding-agent behavior close to the code.",
      "scope": "Add root `AGENTS.md` from the approved instructions. Include reading order, boundaries, naming, verification, and deviation policy.",
      "files": "AGENTS.md",
      "tests": "Validate required headings and snake_case canonical names with a small repository validation script.",
      "commit": "docs(repo): add agent implementation instructions",
      "extra": [],
      "verification_commands": [
        "python3 scripts/validate_spec.py  # once introduced; otherwise run the strongest existing schema/checksum validation",
        "git diff --check",
        "# Discover and run any repository documentation/link/schema validation command introduced by this step."
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 3,
      "step_id": "step_003",
      "phase": "phase_00_spec_and_repository_control",
      "phase_step": 3,
      "title": "Add governance and contribution documents",
      "purpose": "Define ownership, contribution, security reporting, and change review before code.",
      "scope": "Add README status language, CONTRIBUTING, SECURITY, CODEOWNERS, and dual-license files. State no conformance/production claim yet.",
      "files": "README.md; CONTRIBUTING.md; SECURITY.md; CODEOWNERS; LICENSE_MIT; LICENSE_APACHE",
      "tests": "Check links, required sections, and license metadata through repository validation.",
      "commit": "docs(repo): establish governance and security policies",
      "extra": [],
      "verification_commands": [
        "python3 scripts/validate_spec.py  # once introduced; otherwise run the strongest existing schema/checksum validation",
        "git diff --check",
        "# Discover and run any repository documentation/link/schema validation command introduced by this step."
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 4,
      "step_id": "step_004",
      "phase": "phase_00_spec_and_repository_control",
      "phase_step": 4,
      "title": "Add normative NIP snapshot",
      "purpose": "Anchor implementation to one exact NIP draft.",
      "scope": "Store the approved NIP draft unchanged under `spec/`. Add its SHA-256 and a note that prose controls fixtures when they disagree.",
      "files": "spec/NIP_DRAFT.md; spec/NIP_DRAFT.sha256",
      "tests": "Checksum test recomputes and matches the committed digest.",
      "commit": "docs(spec): add normative NIP draft snapshot",
      "extra": [],
      "verification_commands": [
        "python3 scripts/validate_spec.py  # once introduced; otherwise run the strongest existing schema/checksum validation",
        "git diff --check",
        "# Discover and run any repository documentation/link/schema validation command introduced by this step."
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 5,
      "step_id": "step_005",
      "phase": "phase_00_spec_and_repository_control",
      "phase_step": 5,
      "title": "Add companion specification set",
      "purpose": "Preserve implementation invariants too operational for the NIP.",
      "scope": "Add architecture, API, data, Automerge, control, checkpoint, conformance, security, versioning, and acceptance documents without implementation code.",
      "files": "spec/*.md",
      "tests": "Repository validator checks every required companion document exists.",
      "commit": "docs(spec): add companion protocol contracts",
      "extra": [],
      "verification_commands": [
        "python3 scripts/validate_spec.py  # once introduced; otherwise run the strongest existing schema/checksum validation",
        "git diff --check",
        "# Discover and run any repository documentation/link/schema validation command introduced by this step."
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 6,
      "step_id": "step_006",
      "phase": "phase_00_spec_and_repository_control",
      "phase_step": 6,
      "title": "Add machine-readable requirements registry",
      "purpose": "Give every normative behavior a stable implementation/test anchor.",
      "scope": "Import requirements JSON; validate unique IDs, required fields, source references, and category names. Do not mark unimplemented requirements complete.",
      "files": "spec/requirements.json; tools/validation/requirements_schema.json; scripts/validate_requirements.py",
      "tests": "Positive registry validation and negative duplicate/missing-field fixtures.",
      "commit": "test(spec): validate normative requirements registry",
      "extra": [],
      "verification_commands": [
        "python3 scripts/validate_spec.py  # once introduced; otherwise run the strongest existing schema/checksum validation",
        "git diff --check",
        "# Discover and run any repository documentation/link/schema validation command introduced by this step."
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 7,
      "step_id": "step_007",
      "phase": "phase_00_spec_and_repository_control",
      "phase_step": 7,
      "title": "Define protocol revision manifest",
      "purpose": "Make the draft profile identity and status explicit.",
      "scope": "Add machine-readable draft revision metadata including provisional kinds, sealed status, Automerge profile name, limit status, and wire-domain strings.",
      "files": "spec/protocol_revision.json; spec/protocol_revision.schema.json",
      "tests": "Schema validation; reject custom/missing kinds, changed actor domain, and non-draft status.",
      "commit": "docs(spec): define sealed draft protocol revision",
      "extra": [],
      "verification_commands": [
        "python3 scripts/validate_spec.py  # once introduced; otherwise run the strongest existing schema/checksum validation",
        "git diff --check",
        "# Discover and run any repository documentation/link/schema validation command introduced by this step."
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 8,
      "step_id": "step_008",
      "phase": "phase_00_spec_and_repository_control",
      "phase_step": 8,
      "title": "Add ADR index and approved decisions",
      "purpose": "Preserve why key architecture decisions were made.",
      "scope": "Add all approved ADRs and an index linking their status and affected requirements.",
      "files": "docs/adr/*.md; docs/adr/README.md",
      "tests": "Validate ADR numbering, status, and links.",
      "commit": "docs(adr): record approved architecture decisions",
      "extra": [],
      "verification_commands": [
        "python3 scripts/validate_spec.py  # once introduced; otherwise run the strongest existing schema/checksum validation",
        "git diff --check",
        "# Discover and run any repository documentation/link/schema validation command introduced by this step."
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 9,
      "step_id": "step_009",
      "phase": "phase_00_spec_and_repository_control",
      "phase_step": 9,
      "title": "Add prior-art and rejected-alternatives record",
      "purpose": "Prevent future contributors from repeating rejected designs or missing related NIP work.",
      "scope": "Add concise records for NIP-78, PRs 667/2192/1630/2123/400/569/1015, issues 929/419/1670/2147, and the resulting decisions.",
      "files": "docs/research/prior_art.md; docs/research/rejected_alternatives.md",
      "tests": "Link checker and required prior-art identifier check.",
      "commit": "docs(research): record Nostr CRDT prior art",
      "extra": [],
      "verification_commands": [
        "python3 scripts/validate_spec.py  # once introduced; otherwise run the strongest existing schema/checksum validation",
        "git diff --check",
        "# Discover and run any repository documentation/link/schema validation command introduced by this step."
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 10,
      "step_id": "step_010",
      "phase": "phase_00_spec_and_repository_control",
      "phase_step": 10,
      "title": "Define fixture metadata schema",
      "purpose": "Create a neutral executable-spec envelope before implementation.",
      "scope": "Add fixture schema fields for ID, revision, requirements, provenance, raw inputs, expected report, seed, and checksum. Do not add implementation-derived expectations yet.",
      "files": "fixtures/schema/fixture.schema.json; fixtures/README.md",
      "tests": "Validate representative valid fixture and malformed fixture metadata.",
      "commit": "test(fixtures): define language-neutral fixture schema",
      "extra": [],
      "verification_commands": [
        "python3 scripts/validate_spec.py  # once introduced; otherwise run the strongest existing schema/checksum validation",
        "git diff --check",
        "# Discover and run any repository documentation/link/schema validation command introduced by this step."
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 11,
      "step_id": "step_011",
      "phase": "phase_00_spec_and_repository_control",
      "phase_step": 11,
      "title": "Define canonical report schema",
      "purpose": "Fix the cross-language output shape before Rust becomes de facto spec.",
      "scope": "Add schema for canonical controls, dispositions, accepted sets, heads, digests, typed assertions, alerts, and local completion. Exclude local completion from canonical disposition digest.",
      "files": "fixtures/schema/report.schema.json; spec/report_contract.md",
      "tests": "Schema positives/negatives, including rejection of unknown outcome names.",
      "commit": "test(conformance): define canonical report schema",
      "extra": [],
      "verification_commands": [
        "python3 scripts/validate_spec.py  # once introduced; otherwise run the strongest existing schema/checksum validation",
        "git diff --check",
        "# Discover and run any repository documentation/link/schema validation command introduced by this step."
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 12,
      "step_id": "step_012",
      "phase": "phase_00_spec_and_repository_control",
      "phase_step": 12,
      "title": "Define history digest binary contract",
      "purpose": "Eliminate ambiguity in the normative history digest.",
      "scope": "Approve domain string, revision encoding, coordinate encoding, count widths, control chain order, accepted-change order, and head order. Add hand-computed examples.",
      "files": "spec/history_digest.md; fixtures/examples/history_digest_v1.json",
      "tests": "Independent test script recomputes example digest; malformed ordering negative.",
      "commit": "docs(conformance): define normative history digest",
      "extra": [],
      "verification_commands": [
        "python3 scripts/validate_spec.py  # once introduced; otherwise run the strongest existing schema/checksum validation",
        "git diff --check",
        "# Discover and run any repository documentation/link/schema validation command introduced by this step."
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 13,
      "step_id": "step_013",
      "phase": "phase_00_spec_and_repository_control",
      "phase_step": 13,
      "title": "Define dispositions digest binary contract",
      "purpose": "Make protocol outcomes cross-language comparable.",
      "scope": "Approve domain string, identifier namespace, item ordering, disposition numeric codes, and exclusion of local completion. Add examples.",
      "files": "spec/dispositions_digest.md; fixtures/examples/dispositions_digest_v1.json",
      "tests": "Independent digest example verification and duplicate-item rejection.",
      "commit": "docs(conformance): define dispositions digest",
      "extra": [],
      "verification_commands": [
        "python3 scripts/validate_spec.py  # once introduced; otherwise run the strongest existing schema/checksum validation",
        "git diff --check",
        "# Discover and run any repository documentation/link/schema validation command introduced by this step."
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 14,
      "step_id": "step_014",
      "phase": "phase_00_spec_and_repository_control",
      "phase_step": 14,
      "title": "Define stable diagnostic code registry",
      "purpose": "Avoid protocol logic or tests depending on mutable human error strings.",
      "scope": "Add unique machine-readable codes for raw event, NIP-01, carrier, Automerge, control, graph, checkpoint, budget, and cancellation diagnostics.",
      "files": "spec/diagnostic_codes.json; spec/diagnostic_codes.md",
      "tests": "Validate uniqueness, prefix conventions, and no collision with protocol disposition names.",
      "commit": "docs(api): define diagnostic code registry",
      "extra": [],
      "verification_commands": [
        "python3 scripts/validate_spec.py  # once introduced; otherwise run the strongest existing schema/checksum validation",
        "git diff --check",
        "# Discover and run any repository documentation/link/schema validation command introduced by this step."
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 15,
      "step_id": "step_015",
      "phase": "phase_00_spec_and_repository_control",
      "phase_step": 15,
      "title": "Define draft limits registry",
      "purpose": "Centralize provisional normative limits and distinguish them from WorkBudget.",
      "scope": "Add machine-readable draft limits with units, requirement IDs, and provisional status. Include checkpoint values but mark production qualification pending.",
      "files": "spec/draft_limits.json; spec/draft_limits.md",
      "tests": "Schema/range validation and one-over-limit fixture metadata.",
      "commit": "docs(spec): centralize provisional draft limits",
      "extra": [],
      "verification_commands": [
        "python3 scripts/validate_spec.py  # once introduced; otherwise run the strongest existing schema/checksum validation",
        "git diff --check",
        "# Discover and run any repository documentation/link/schema validation command introduced by this step."
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 16,
      "step_id": "step_016",
      "phase": "phase_00_spec_and_repository_control",
      "phase_step": 16,
      "title": "Add baseline handoff validator and report",
      "purpose": "Prove the specification baseline is internally complete before code.",
      "scope": "Create xtask/script that validates names, required files, schemas, unique requirements, checksums, forbidden deprecated terms, and sealed profile fields. Commit a clean baseline report.",
      "files": "scripts/validate_spec.py; reports/spec_baseline.txt",
      "tests": "Run validator twice and require deterministic output.",
      "commit": "test(spec): add deterministic baseline validation",
      "extra": [],
      "verification_commands": [
        "python3 scripts/validate_spec.py  # once introduced; otherwise run the strongest existing schema/checksum validation",
        "git diff --check",
        "# Discover and run any repository documentation/link/schema validation command introduced by this step."
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 17,
      "step_id": "step_017",
      "phase": "phase_01_workspace_and_core_types",
      "phase_step": 1,
      "title": "Create the snake_case Cargo workspace",
      "purpose": "Establish the approved repository layout.",
      "scope": "Create root workspace with members `crates/nostr_automerge`, `tools/nostr_automerge_conformance`, and `tools/nostr_automerge_xtask`. Set resolver, package metadata, license, repository, and initial toolchain/MSRV decisions.",
      "files": "Cargo.toml; Cargo.lock; rust-toolchain.toml; crates/; tools/",
      "tests": "Cargo metadata resolves; workspace contains only approved members.",
      "commit": "build(repo): initialize nostr_automerge workspace",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge create_the_snake_case_cargo_workspace --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 18,
      "step_id": "step_018",
      "phase": "phase_01_workspace_and_core_types",
      "phase_step": 2,
      "title": "Create the public library skeleton",
      "purpose": "Introduce one publishable crate without protocol behavior.",
      "scope": "Add `crates/nostr_automerge` package, lib root, crate docs, forbidding unsafe code, and placeholder private modules only where required.",
      "files": "crates/nostr_automerge/Cargo.toml; crates/nostr_automerge/src/lib.rs",
      "tests": "Compile crate; doctest crate-level status/non-goal example.",
      "commit": "feat(core): add nostr_automerge library skeleton",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge create_the_public_library_skeleton --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 19,
      "step_id": "step_019",
      "phase": "phase_01_workspace_and_core_types",
      "phase_step": 3,
      "title": "Create private conformance tool skeleton",
      "purpose": "Reserve the private CLI boundary without implementing semantics.",
      "scope": "Add non-publishable `nostr_automerge_conformance` binary with `--help` and explicit not-yet-implemented status.",
      "files": "tools/nostr_automerge_conformance/Cargo.toml; src/main.rs",
      "tests": "CLI help smoke test; no network dependencies.",
      "commit": "build(tools): add conformance tool skeleton",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge create_private_conformance_tool_skeleton --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 20,
      "step_id": "step_020",
      "phase": "phase_01_workspace_and_core_types",
      "phase_step": 4,
      "title": "Create private xtask skeleton",
      "purpose": "Provide one repository automation entry point.",
      "scope": "Add non-publishable `nostr_automerge_xtask` binary with validation subcommand routing. Keep it std-only where practical.",
      "files": "tools/nostr_automerge_xtask/Cargo.toml; src/main.rs",
      "tests": "xtask help and unknown-command tests.",
      "commit": "build(tools): add xtask skeleton",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge create_private_xtask_skeleton --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 21,
      "step_id": "step_021",
      "phase": "phase_01_workspace_and_core_types",
      "phase_step": 5,
      "title": "Configure workspace lint policy",
      "purpose": "Enforce approved code health without blanket lint churn.",
      "scope": "Add workspace Rust/rustdoc/Clippy lints, rustfmt, clippy config, and targeted deny rules. Allow trusted fixture tests to use explicit expectations if policy requires.",
      "files": "Cargo.toml; rustfmt.toml; clippy.toml",
      "tests": "Add a compile-fail or policy check proving unsafe is rejected; run all lints.",
      "commit": "build(lints): enforce Rust quality policy",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge configure_workspace_lint_policy --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 22,
      "step_id": "step_022",
      "phase": "phase_01_workspace_and_core_types",
      "phase_step": 6,
      "title": "Add fixed 32-byte semantic base type",
      "purpose": "Avoid repeated unsafe/stringly parsing across identifiers.",
      "scope": "Implement an internal fixed-byte helper with constant size, byte ordering, redacted Debug policy, and checked construction. Do not expose one generic public identifier type.",
      "files": "crates/nostr_automerge/src/types/fixed_32.rs; types/mod.rs",
      "tests": "Length, ordering, copy/clone, redacted debug, and invalid construction tests.",
      "commit": "feat(types): add fixed 32-byte identifier foundation",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge add_fixed_32_byte_semantic_base_type --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 23,
      "step_id": "step_023",
      "phase": "phase_01_workspace_and_core_types",
      "phase_step": 7,
      "title": "Add strict lowercase hexadecimal codec",
      "purpose": "Canonicalize all 32-byte text boundaries.",
      "scope": "Implement allocation-bounded lowercase hex decode/encode with exact length and no uppercase acceptance.",
      "files": "src/wire/hex.rs; src/error.rs",
      "tests": "Valid roundtrip; uppercase, odd, nonhex, short, long negatives.",
      "commit": "feat(wire): add strict lowercase hex codec",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge add_strict_lowercase_hexadecimal_codec --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 24,
      "step_id": "step_024",
      "phase": "phase_01_workspace_and_core_types",
      "phase_step": 8,
      "title": "Add event and key newtypes",
      "purpose": "Represent Nostr identities semantically.",
      "scope": "Add EventId, ControllerPublicKey, DevicePublicKey, AccountPublicKey, and generic verified PublicKey internal conversion. Implement byte order and strict hex APIs.",
      "files": "src/types/event_id.rs; public_key.rs; types/mod.rs",
      "tests": "Cross-type noninterchangeability compile/API tests and codecs.",
      "commit": "feat(types): add Nostr event and key identifiers",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge add_event_and_key_newtypes --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 25,
      "step_id": "step_025",
      "phase": "phase_01_workspace_and_core_types",
      "phase_step": 9,
      "title": "Add document and Automerge identifier newtypes",
      "purpose": "Represent document/CRDT hashes without strings.",
      "scope": "Add DocumentId, ActorId, ChangeHash, SnapshotHash, ChunkHash, HistoryDigest, and DispositionsDigest.",
      "files": "src/types/document_id.rs; actor_id.rs; change_hash.rs; digest.rs",
      "tests": "Exact bytes/hex/order tests for every type.",
      "commit": "feat(types): add document and Automerge identifiers",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge add_document_and_automerge_identifier_newtypes --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 26,
      "step_id": "step_026",
      "phase": "phase_01_workspace_and_core_types",
      "phase_step": 10,
      "title": "Add DocumentCoordinate",
      "purpose": "Create the stable protocol identity object.",
      "scope": "Implement strict controller+document coordinate, draft NIP-01 address rendering/parsing through sealed profile, and no relay data.",
      "files": "src/types/document_coordinate.rs",
      "tests": "Roundtrip, wrong kind, malformed colon/hex, and byte-order tests.",
      "commit": "feat(types): add document coordinate",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge add_documentcoordinate --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 27,
      "step_id": "step_027",
      "phase": "phase_01_workspace_and_core_types",
      "phase_step": 11,
      "title": "Add sealed ProtocolRevision",
      "purpose": "Prevent caller-defined protocol behavior.",
      "scope": "Implement nonconstructible profile lookup for Draft2026_08 only. Keep kind/limit structs private and return read-only semantic accessors where needed.",
      "files": "src/profile.rs",
      "tests": "Cannot construct custom profile; revision parsing/formatting; unknown revision result.",
      "commit": "feat(profile): add sealed protocol revision",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge add_sealed_protocolrevision --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 28,
      "step_id": "step_028",
      "phase": "phase_01_workspace_and_core_types",
      "phase_step": 12,
      "title": "Add provisional event-kind constants",
      "purpose": "Centralize draft kind allocation.",
      "scope": "Define private constants and classification for 1624/1625/1626/1627/31624 under Draft2026_08. No scattered literals.",
      "files": "src/profile/kinds.rs",
      "tests": "Classification and unknown-kind tests; source scan for duplicate literals if practical.",
      "commit": "feat(profile): add provisional draft event kinds",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge add_provisional_event_kind_constants --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 29,
      "step_id": "step_029",
      "phase": "phase_01_workspace_and_core_types",
      "phase_step": 13,
      "title": "Add sealed normative protocol limits",
      "purpose": "Make draft validity limits consistent across callers.",
      "scope": "Implement ProtocolLimits from spec/draft_limits.json as private revision data with typed units and checked conversions.",
      "files": "src/limits.rs; build/validation link to spec/draft_limits.json",
      "tests": "Every machine limit maps once; boundary values and one-over tests.",
      "commit": "feat(profile): add sealed draft protocol limits",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge add_sealed_normative_protocol_limits --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 30,
      "step_id": "step_030",
      "phase": "phase_01_workspace_and_core_types",
      "phase_step": 14,
      "title": "Add deterministic WorkBudget and cancellation contracts",
      "purpose": "Separate local execution capacity from validity.",
      "scope": "Implement WorkBudget counters and CancellationCheck trait without Instant/system clock. Provide unlimited-for-test constructor only under approved visibility.",
      "files": "src/work_budget.rs",
      "tests": "Counter exhaustion, checked increments, cancellation, and no protocol-disposition mutation tests.",
      "commit": "feat(core): add work budget and cancellation",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge add_deterministic_workbudget_and_cancellation_cont --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 31,
      "step_id": "step_031",
      "phase": "phase_01_workspace_and_core_types",
      "phase_step": 15,
      "title": "Add protocol disposition and completion enums",
      "purpose": "Fix canonical outcome vocabulary.",
      "scope": "Implement Accepted/Pending/Excluded/Invalid/UnsupportedRevision and Complete/BudgetExhausted/Cancelled with stable numeric/report encodings.",
      "files": "src/disposition.rs; src/report.rs",
      "tests": "Serialization/order/code tests; forbid deprecated unsupported/resource-refused strings.",
      "commit": "feat(report): add disposition and completion types",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge add_protocol_disposition_and_completion_enums --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 32,
      "step_id": "step_032",
      "phase": "phase_01_workspace_and_core_types",
      "phase_step": 16,
      "title": "Add integrity alert and diagnostic foundations",
      "purpose": "Create typed security/integrity reporting before evaluator logic.",
      "scope": "Implement alert enum shapes for controller equivocation, reorganization, and device equivocation plus stable diagnostic code wrapper.",
      "files": "src/integrity.rs; src/diagnostic.rs; src/report.rs",
      "tests": "Stable field ordering/serialization and privacy-safe Debug tests.",
      "commit": "feat(report): add integrity alert foundations",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge add_integrity_alert_and_diagnostic_foundations --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 33,
      "step_id": "step_033",
      "phase": "phase_02_strict_wire_and_nip01",
      "phase_step": 1,
      "title": "Add bounded raw event input",
      "purpose": "Reject oversized/untrusted bytes before expensive parsing.",
      "scope": "Implement RawEventBytes checked wrapper using sealed raw event limit and UTF-8 validation boundary.",
      "files": "src/wire/raw_event.rs",
      "tests": "Empty, valid UTF-8, invalid UTF-8, exact-limit, over-limit tests.",
      "commit": "feat(wire): bound raw NIP-01 event input",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge add_bounded_raw_event_input --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 34,
      "step_id": "step_034",
      "phase": "phase_02_strict_wire_and_nip01",
      "phase_step": 2,
      "title": "Implement duplicate-member JSON scanner",
      "purpose": "Preserve information lost by ordinary deserialization.",
      "scope": "Implement a bounded scanner/parser that rejects duplicate top-level NIP-01 members before semantic parse and consumes exactly one JSON value.",
      "files": "src/wire/strict_json.rs",
      "tests": "Duplicate id/content/tags cases, escaped keys, trailing JSON, depth/size negatives.",
      "commit": "feat(wire): reject duplicate NIP-01 members",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge implement_duplicate_member_json_scanner --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 35,
      "step_id": "step_035",
      "phase": "phase_02_strict_wire_and_nip01",
      "phase_step": 3,
      "title": "Implement exact NIP-01 event shape parsing",
      "purpose": "Validate field presence and types deterministically.",
      "scope": "Parse id, pubkey, created_at, kind, tags, content, sig; reject missing fields, wrong types, out-of-range kind/timestamp, and unsupported extra top-level members according to approved boundary.",
      "files": "src/wire/nip01/raw.rs; src/wire/nip01/mod.rs",
      "tests": "Positive event and per-field malformed fixtures.",
      "commit": "feat(nip01): parse strict event shape",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge implement_exact_nip_01_event_shape_parsing --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 36,
      "step_id": "step_036",
      "phase": "phase_02_strict_wire_and_nip01",
      "phase_step": 4,
      "title": "Implement exact tag-array parsing",
      "purpose": "Stop malformed tag structures before carrier validation.",
      "scope": "Require tags as arrays containing at least one string and only strings; preserve exact element order/bytes.",
      "files": "src/wire/nip01/tags.rs",
      "tests": "Empty tag, null/non-string, nested array, large tag count/length boundary tests.",
      "commit": "feat(nip01): parse strict Nostr tags",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge implement_exact_tag_array_parsing --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 37,
      "step_id": "step_037",
      "phase": "phase_02_strict_wire_and_nip01",
      "phase_step": 5,
      "title": "Implement signature and identifier codecs",
      "purpose": "Validate fixed-width event fields before cryptography.",
      "scope": "Add exact 64-byte signature lowercase-hex type and use semantic EventId/PublicKey codecs.",
      "files": "src/types/signature.rs; src/wire/nip01/raw.rs",
      "tests": "Length, uppercase, invalid scalar/point prevalidation as appropriate.",
      "commit": "feat(nip01): add strict signature codecs",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge implement_signature_and_identifier_codecs --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 38,
      "step_id": "step_038",
      "phase": "phase_02_strict_wire_and_nip01",
      "phase_step": 6,
      "title": "Implement canonical NIP-01 serialization",
      "purpose": "Calculate event IDs without third-party serialization ambiguity.",
      "scope": "Serialize `[0,pubkey,created_at,kind,tags,content]` with exact escaping and UTF-8 rules from NIP-01.",
      "files": "src/wire/nip01/serialize.rs",
      "tests": "Official/basic vectors; control characters; Unicode; empty tags/content; deterministic bytes.",
      "commit": "feat(nip01): implement canonical event serialization",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge implement_canonical_nip_01_serialization --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 39,
      "step_id": "step_039",
      "phase": "phase_02_strict_wire_and_nip01",
      "phase_step": 7,
      "title": "Implement event ID calculation",
      "purpose": "Bind raw semantic event fields to SHA-256.",
      "scope": "Calculate EventId from canonical NIP-01 serialization and compare with declared id using constant semantic equality.",
      "files": "src/wire/nip01/verify.rs",
      "tests": "Valid vector and one-bit field/id mismatch tests.",
      "commit": "feat(nip01): verify event identifiers",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge implement_event_id_calculation --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 40,
      "step_id": "step_040",
      "phase": "phase_02_strict_wire_and_nip01",
      "phase_step": 8,
      "title": "Add private BIP-340 verification adapter",
      "purpose": "Verify signatures through a narrow replaceable dependency boundary.",
      "scope": "Select the approved low-level library, isolate public-key/signature verification, map errors to stable diagnostics, and expose no third-party types publicly.",
      "files": "src/crypto/bip340.rs; Cargo.toml; docs/adr/dependency_selection.md",
      "tests": "Official BIP-340 valid/invalid vectors and differential test oracle where available.",
      "commit": "feat(crypto): add BIP-340 verification adapter",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge add_private_bip_340_verification_adapter --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 41,
      "step_id": "step_041",
      "phase": "phase_02_strict_wire_and_nip01",
      "phase_step": 9,
      "title": "Create VerifiedNip01Event",
      "purpose": "Expose only fully verified NIP-01 evidence to carrier parsing.",
      "scope": "Combine shape, EventId, and signature verification into an immutable verified event with retained raw bytes and semantic fields.",
      "files": "src/wire/nip01/verified.rs; src/wire/nip01/mod.rs",
      "tests": "End-to-end valid event; each verification failure; raw bytes retained.",
      "commit": "feat(nip01): add verified event boundary",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge create_verifiednip01event --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 42,
      "step_id": "step_042",
      "phase": "phase_02_strict_wire_and_nip01",
      "phase_step": 10,
      "title": "Implement strict padded base64",
      "purpose": "Reject alternate binary encodings.",
      "scope": "Add standard RFC4648 padded encoder/decoder with predecode size limits; reject URL-safe, whitespace, unpadded, and noncanonical forms.",
      "files": "src/wire/base64.rs",
      "tests": "RFC vectors plus all forbidden variants and allocation boundaries.",
      "commit": "feat(wire): add strict padded base64",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge implement_strict_padded_base64 --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 43,
      "step_id": "step_043",
      "phase": "phase_02_strict_wire_and_nip01",
      "phase_step": 11,
      "title": "Add RFC 8785 canonical serializer adapter",
      "purpose": "Provide a qualified canonical JSON output path.",
      "scope": "Select or implement reviewed JCS serializer for supported closed objects; isolate dependency and enforce no float/unsafe integer.",
      "files": "src/wire/canonical_json/serialize.rs; Cargo.toml",
      "tests": "RFC 8785 vectors relevant to strings/integers/member ordering.",
      "commit": "feat(wire): add canonical JSON serializer",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge add_rfc_8785_canonical_serializer_adapter --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 44,
      "step_id": "step_044",
      "phase": "phase_02_strict_wire_and_nip01",
      "phase_step": 12,
      "title": "Add duplicate-rejecting canonical content parser",
      "purpose": "Validate signed JCS object bytes rather than normalizing them.",
      "scope": "Parse content with duplicate-key rejection at all object depths, reject floats/unsafe integers, then require exact canonical reserialization.",
      "files": "src/wire/canonical_json/parse.rs",
      "tests": "Nested duplicates, member order, escapes, integer boundaries, float negatives.",
      "commit": "feat(wire): validate canonical JSON content",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge add_duplicate_rejecting_canonical_content_parser --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 45,
      "step_id": "step_045",
      "phase": "phase_02_strict_wire_and_nip01",
      "phase_step": 13,
      "title": "Add exact required-tag helpers",
      "purpose": "Share strict tag cardinality without permissive carrier parsers.",
      "scope": "Implement helpers for exactly-one tag, absent tag, exact element count, sorted/unique tag value constraints where specified.",
      "files": "src/wire/tags.rs",
      "tests": "Missing/repeated/extra-element/malformed values and unknown-tag ignore tests.",
      "commit": "feat(wire): add exact tag validation helpers",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge add_exact_required_tag_helpers --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 46,
      "step_id": "step_046",
      "phase": "phase_02_strict_wire_and_nip01",
      "phase_step": 14,
      "title": "Add URL and scalar content validators",
      "purpose": "Support manifest fields with deterministic constraints.",
      "scope": "Implement absolute ws/wss URL validation, printable ASCII, UTF-8 byte-length, sorted uniqueness, safe integer and nullable field helpers. Use a standards-aware URL parser privately.",
      "files": "src/wire/scalars.rs; Cargo.toml",
      "tests": "IPv4/IPv6/ws/wss, invalid schemes/credentials/fragments as specified, length and sort tests.",
      "commit": "feat(wire): add manifest scalar validators",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge add_url_and_scalar_content_validators --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 47,
      "step_id": "step_047",
      "phase": "phase_02_strict_wire_and_nip01",
      "phase_step": 15,
      "title": "Map wire failures to stable diagnostics",
      "purpose": "Make wire behavior machine-readable without string matching.",
      "scope": "Map every raw/NIP-01/base64/JCS/tag failure to registered diagnostic codes and privacy-safe context.",
      "files": "src/diagnostic.rs; src/wire/error.rs",
      "tests": "Registry coverage test proves no unregistered emitted code.",
      "commit": "feat(wire): stabilize wire diagnostics",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge map_wire_failures_to_stable_diagnostics --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 48,
      "step_id": "step_048",
      "phase": "phase_02_strict_wire_and_nip01",
      "phase_step": 16,
      "title": "Add raw NIP-01 conformance fixture suite",
      "purpose": "Lock the complete strict event boundary before carriers.",
      "scope": "Add raw valid/invalid fixture files, fixture runner integration, official signature vectors, duplicate-key and malformed encoding corpus.",
      "files": "fixtures/v1_draft/nip01/; tests/nip01_conformance.rs",
      "tests": "Run all fixtures in canonical/reverse file order and compare expected diagnostics.",
      "commit": "test(nip01): add strict raw event conformance",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge add_raw_nip_01_conformance_fixture_suite --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 49,
      "step_id": "step_049",
      "phase": "phase_03_automerge_qualification",
      "phase_step": 1,
      "title": "Record and pin the Automerge dependency",
      "purpose": "Make the upstream behavior reproducible and reviewed.",
      "scope": "Re-check candidate version/source, create dependency ADR, pin exact version, commit lockfile, and record feature selection. Do not enable implicit UTF feature shortcuts as a substitute for explicit options.",
      "files": "Cargo.toml; Cargo.lock; docs/adr/automerge_dependency.md",
      "tests": "Build at MSRV and stable; dependency metadata test/report.",
      "commit": "build(automerge): pin qualified upstream dependency",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge record_and_pin_the_automerge_dependency --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 50,
      "step_id": "step_050",
      "phase": "phase_03_automerge_qualification",
      "phase_step": 2,
      "title": "Create the Automerge anti-corruption adapter",
      "purpose": "Centralize every upstream call.",
      "scope": "Add private adapter module and forbid direct `automerge::` references outside it via repository validation/search.",
      "files": "src/automerge_adapter/mod.rs; scripts/validate_architecture.py",
      "tests": "Architecture test detects a deliberate forbidden direct reference fixture.",
      "commit": "refactor(automerge): add anti-corruption adapter",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge create_the_automerge_anti_corruption_adapter --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 51,
      "step_id": "step_051",
      "phase": "phase_03_automerge_qualification",
      "phase_step": 3,
      "title": "Validate change magic and chunk type",
      "purpose": "Reject non-change inputs before Automerge.",
      "scope": "Implement fixed-header inspection for magic and require type 0x01 without decompression or upstream parse.",
      "files": "src/automerge_adapter/framing.rs",
      "tests": "Valid change prefix; document/compressed/bundle/unknown type negatives.",
      "commit": "feat(automerge): validate change magic and type",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge validate_change_magic_and_chunk_type --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 52,
      "step_id": "step_052",
      "phase": "phase_03_automerge_qualification",
      "phase_step": 4,
      "title": "Implement shortest uLEB128 decoding",
      "purpose": "Prevent alternate encodings and integer overflow.",
      "scope": "Add bounded u64 decoder that returns consumed bytes and rejects overlong/non-shortest/overflow/truncated values.",
      "files": "src/automerge_adapter/leb128.rs",
      "tests": "Boundary values, shortest vectors, 10+ byte, overflow, continuation negatives.",
      "commit": "feat(automerge): enforce shortest uleb128 lengths",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge implement_shortest_uleb128_decoding --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 53,
      "step_id": "step_053",
      "phase": "phase_03_automerge_qualification",
      "phase_step": 5,
      "title": "Validate declared length and trailing bytes",
      "purpose": "Require one exact chunk.",
      "scope": "Use checked u64-to-usize conversion, sealed change limit, exact remaining length, and no trailing bytes.",
      "files": "src/automerge_adapter/framing.rs",
      "tests": "Exact, short, long, trailing, platform-conversion, one-over-limit tests.",
      "commit": "feat(automerge): validate exact change length",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge validate_declared_length_and_trailing_bytes --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 54,
      "step_id": "step_054",
      "phase": "phase_03_automerge_qualification",
      "phase_step": 6,
      "title": "Validate checksum and ChangeHash",
      "purpose": "Bind framing to exact canonical change identity.",
      "scope": "Calculate SHA-256 over type + shortest length + contents; compare first four checksum bytes and expose ChangeHash.",
      "files": "src/automerge_adapter/framing.rs; src/types/change_hash.rs",
      "tests": "Hand-computed vector, altered checksum/content/length negatives.",
      "commit": "feat(automerge): validate checksum and change hash",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge validate_checksum_and_changehash --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 55,
      "step_id": "step_055",
      "phase": "phase_03_automerge_qualification",
      "phase_step": 7,
      "title": "Reject forbidden chunk/column forms before semantic use",
      "purpose": "Ensure compressed/document/bundle forms never reach normal change path.",
      "scope": "Add explicit diagnostics and raw fixtures for chunk types and compressed-column semantic rejection.",
      "files": "src/automerge_adapter/framing.rs; fixtures/v1_draft/automerge_framing/",
      "tests": "All forbidden forms fail before decode; instrumentation/test proves no upstream decode called.",
      "commit": "test(automerge): reject forbidden chunk forms",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge reject_forbidden_chunk_column_forms_before_semanti --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 56,
      "step_id": "step_056",
      "phase": "phase_03_automerge_qualification",
      "phase_step": 8,
      "title": "Create Automerge documents with explicit UTF-16",
      "purpose": "Eliminate native/WASM indexing default divergence.",
      "scope": "Add adapter constructor using explicit UTF-16 code-unit indexing only.",
      "files": "src/automerge_adapter/document.rs",
      "tests": "Emoji/surrogate-rich text edits and index assertions.",
      "commit": "feat(automerge): create explicit utf16 documents",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge create_automerge_documents_with_explicit_utf_16 --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 57,
      "step_id": "step_057",
      "phase": "phase_03_automerge_qualification",
      "phase_step": 9,
      "title": "Load with UTF-16, no migration, and no partial state",
      "purpose": "Prevent validation from mutating or partially accepting snapshots.",
      "scope": "Configure explicit load options: UTF-16, no string migration, partial load error, head verification. Wrap errors.",
      "files": "src/automerge_adapter/document.rs",
      "tests": "Load valid save; string-migration fixture unchanged; truncated save rejected; head checks.",
      "commit": "feat(automerge): harden document loading",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge load_with_utf_16_no_migration_and_no_partial_state --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 58,
      "step_id": "step_058",
      "phase": "phase_03_automerge_qualification",
      "phase_step": 10,
      "title": "Prove derived actor replaces unused random actor",
      "purpose": "Ensure ambient randomness never enters protocol changes.",
      "scope": "Set caller-provided ActorId before transaction; inspect produced actors/change graph and assert random initial actor absent.",
      "files": "src/automerge_adapter/document.rs; tests/automerge_actor.rs",
      "tests": "Repeat with fixed input across processes/seeds; only derived actor appears.",
      "commit": "test(automerge): exclude unused random actor",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge prove_derived_actor_replaces_unused_random_actor --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 59,
      "step_id": "step_059",
      "phase": "phase_03_automerge_qualification",
      "phase_step": 11,
      "title": "Generate one canonical uncompressed change",
      "purpose": "Establish a permanent known-good Automerge fixture.",
      "scope": "Create fixed actor/document operation with time 0, no message/extra bytes, extract raw uncompressed bytes and metadata.",
      "files": "tests/support/automerge_fixture_generator.rs; fixtures/v1_draft/automerge_changes/basic/",
      "tests": "Fixture checksum, ChangeHash, decoded fields, reparse/application.",
      "commit": "test(automerge): add canonical basic change fixture",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge generate_one_canonical_uncompressed_change --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 60,
      "step_id": "step_060",
      "phase": "phase_03_automerge_qualification",
      "phase_step": 12,
      "title": "Decode mandatory change metadata and semantics",
      "purpose": "Expose protocol-required fields through the adapter.",
      "scope": "Map actor, seq, start_op, deps, operation count, action/object/scalar/mark semantics, time/message/extra bytes into internal types.",
      "files": "src/automerge_adapter/decode.rs; src/automerge_adapter/types.rs",
      "tests": "Basic and multi-actor/dependency metadata fixtures; unknown semantic negative.",
      "commit": "feat(automerge): decode profiled change semantics",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge decode_mandatory_change_metadata_and_semantics --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 61,
      "step_id": "step_061",
      "phase": "phase_03_automerge_qualification",
      "phase_step": 13,
      "title": "Implement checked actor counter transitions",
      "purpose": "Make sequence and next_op rules explicit and safe.",
      "scope": "Add pure transition function for nonempty/empty changes, checked overflow, start_op requirement, and predecessor sequence requirement inputs.",
      "files": "src/automerge_adapter/counters.rs",
      "tests": "Initial, subsequent, empty, gap, rollback, wrong start_op, overflow tests. Never call max_op on empty.",
      "commit": "feat(automerge): validate actor sequence and op counters",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge implement_checked_actor_counter_transitions --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 62,
      "step_id": "step_062",
      "phase": "phase_03_automerge_qualification",
      "phase_step": 14,
      "title": "Qualify canonical uncompressed re-encoding",
      "purpose": "Prove or block the NIP canonicality rule.",
      "scope": "Implement the selected fallible semantic re-encode path, require raw bytes equality, document any upstream limitations, and fail closed. Do not use catch_unwind as acceptance.",
      "files": "src/automerge_adapter/encode.rs; tests/automerge_reencode.rs; docs/qualification/automerge_reencode.md",
      "tests": "Mandatory semantics, actor/dependency arrangements, empty change, byte equality, negative noncanonical examples; explicit panic-path review.",
      "commit": "feat(automerge): qualify canonical change reencoding",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge qualify_canonical_uncompressed_re_encoding --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 63,
      "step_id": "step_063",
      "phase": "phase_03_automerge_qualification",
      "phase_step": 15,
      "title": "Add complete Automerge semantic matrix",
      "purpose": "Cover every v1 semantic before control/change engine depends on it.",
      "scope": "Add fixtures/tests for map/list/text/table, set/delete/increment, counter, bytes, bool, null, signed/unsigned ints, timestamp, f64 exact bits, marks, Unicode, other actors, empty merge.",
      "files": "fixtures/v1_draft/automerge_semantics/; tests/automerge_semantics.rs",
      "tests": "Decode, re-encode, apply, heads, typed assertion expectations.",
      "commit": "test(automerge): add v1 semantic qualification matrix",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge add_complete_automerge_semantic_matrix --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 64,
      "step_id": "step_064",
      "phase": "phase_03_automerge_qualification",
      "phase_step": 16,
      "title": "Add Automerge profile fuzz and qualification report",
      "purpose": "Stress the highest-risk upstream boundary before proceeding.",
      "scope": "Create fuzz targets for framing/decode/re-encode and a deterministic qualification report covering all gates. Block later profile claim if unresolved.",
      "files": "fuzz/fuzz_targets/automerge_framing.rs; automerge_decode.rs; reports/automerge_qualification.json",
      "tests": "Fuzz smoke; no panic; report schema/checksum; all mandatory gates pass.",
      "commit": "test(automerge): harden and report profile qualification",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge add_automerge_profile_fuzz_and_qualification_repor --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 65,
      "step_id": "step_065",
      "phase": "phase_04_carriers_and_evidence",
      "phase_step": 1,
      "title": "Add carrier classification",
      "purpose": "Route verified events by sealed draft kind without semantic evaluation.",
      "scope": "Implement VerifiedCarrier enum and classifier for manifest/control/change/checkpoint kinds and unsupported revision handling.",
      "files": "src/carrier/mod.rs; src/carrier/classify.rs",
      "tests": "Each kind, unknown kind ignored/not-carrier, wrong revision/profile cases.",
      "commit": "feat(carrier): classify protocol events",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge add_carrier_classification --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 66,
      "step_id": "step_066",
      "phase": "phase_04_carriers_and_evidence",
      "phase_step": 2,
      "title": "Add manifest content model",
      "purpose": "Represent the advisory discovery object exactly.",
      "scope": "Define closed manifest v1 fields and semantic types without selecting state.",
      "files": "src/carrier/manifest.rs",
      "tests": "JCS parse/serialize fixture; null/optional field boundaries.",
      "commit": "feat(manifest): add typed manifest model",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge add_manifest_content_model --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 67,
      "step_id": "step_067",
      "phase": "phase_04_carriers_and_evidence",
      "phase_step": 3,
      "title": "Validate manifests and addressable selection input",
      "purpose": "Enforce manifest constraints while keeping it advisory.",
      "scope": "Validate d tag/controller author, format/encoding/status, relay URL sorting/uniqueness, application metadata, pointers, and forbidden tags.",
      "files": "src/carrier/manifest.rs",
      "tests": "Valid/invalid manifest fixtures, pointer does not authorize/select tests.",
      "commit": "feat(manifest): validate advisory document manifests",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge validate_manifests_and_addressable_selection_input --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 68,
      "step_id": "step_068",
      "phase": "phase_04_carriers_and_evidence",
      "phase_step": 4,
      "title": "Add control content model",
      "purpose": "Represent complete ACL epochs.",
      "scope": "Define closed control v1 content, sequence, base_heads, devices/grants, terminal/continuity fields, profile binding.",
      "files": "src/carrier/control.rs",
      "tests": "Canonical content parse/serialize and field-boundary tests.",
      "commit": "feat(control): add typed control carrier model",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge add_control_content_model --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 69,
      "step_id": "step_069",
      "phase": "phase_04_carriers_and_evidence",
      "phase_step": 5,
      "title": "Validate device grants and role syntax",
      "purpose": "Prepare exact ACL inputs for control semantics.",
      "scope": "Validate sorted unique devices, immutable account fields shape, sorted unique nonempty write/checkpoint roles, derived ActorId consistency fields.",
      "files": "src/carrier/control.rs; src/types/role.rs",
      "tests": "Duplicate/unsorted/unknown role, malformed device/account/actor fixtures.",
      "commit": "feat(control): validate device grants",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge validate_device_grants_and_role_syntax --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 70,
      "step_id": "step_070",
      "phase": "phase_04_carriers_and_evidence",
      "phase_step": 6,
      "title": "Parse and validate change carriers",
      "purpose": "Join signed Nostr metadata with canonical Automerge change.",
      "scope": "Validate a/e/x tags, strict base64, framing, declared hash, internal metadata/profile, forbidden tags, and author device identity.",
      "files": "src/carrier/change.rs",
      "tests": "Positive canonical carrier and wrong coordinate/control/hash/author/framing fixtures.",
      "commit": "feat(change): parse verified change carriers",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge parse_and_validate_change_carriers --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 71,
      "step_id": "step_071",
      "phase": "phase_04_carriers_and_evidence",
      "phase_step": 7,
      "title": "Enforce protocol revision/profile semantics",
      "purpose": "Apply invalid versus unsupported_revision consistently.",
      "scope": "Route unknown declared revision/profile to UnsupportedRevision; route known-v1 unknown properties/semantics to Invalid.",
      "files": "src/carrier/version.rs; src/disposition.rs",
      "tests": "Unknown revision/profile and known-v1 unknown field/action fixtures.",
      "commit": "feat(carrier): enforce revision semantics",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge enforce_protocol_revision_profile_semantics --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 72,
      "step_id": "step_072",
      "phase": "phase_04_carriers_and_evidence",
      "phase_step": 8,
      "title": "Add immutable event evidence records",
      "purpose": "Preserve raw provenance and diagnostics separate from canonical state.",
      "scope": "Define EventEvidence variants for verified carrier, invalid event, unsupported revision, irrelevant event, duplicate event with raw checksum.",
      "files": "src/evidence/event.rs",
      "tests": "Privacy-safe debug; stable diagnostic fields; raw identity retention.",
      "commit": "feat(evidence): add immutable event records",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge add_immutable_event_evidence_records --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 73,
      "step_id": "step_073",
      "phase": "phase_04_carriers_and_evidence",
      "phase_step": 9,
      "title": "Implement CorpusBuilder idempotent ingestion",
      "purpose": "Collect evidence without order-dependent evaluation.",
      "scope": "Add raw ingest pipeline, deduplicate by EventId, preserve first exact raw bytes, diagnose same-ID impossible/malformed mismatch safely.",
      "files": "src/evidence/corpus_builder.rs",
      "tests": "Repeated input, shuffled input, invalid then valid different IDs, idempotence tests.",
      "commit": "feat(evidence): build idempotent evidence corpus",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge implement_corpusbuilder_idempotent_ingestion --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 74,
      "step_id": "step_074",
      "phase": "phase_04_carriers_and_evidence",
      "phase_step": 10,
      "title": "Index controls deterministically",
      "purpose": "Prepare batch control-tree evaluation.",
      "scope": "Build controls_by_id, genesis candidates, children_by_parent with BTree ordering; include pending/invalid separately.",
      "files": "src/evidence/indexes.rs",
      "tests": "Insertion-order permutations produce identical indexes.",
      "commit": "feat(evidence): index control candidates",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge index_controls_deterministically --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 75,
      "step_id": "step_075",
      "phase": "phase_04_carriers_and_evidence",
      "phase_step": 11,
      "title": "Index change carriers by ChangeHash",
      "purpose": "Support multiple carriers without poisoning.",
      "scope": "Build ChangeHash to valid/invalid carrier EventId sets and control/actor/dependency auxiliary indexes.",
      "files": "src/evidence/indexes.rs",
      "tests": "Multiple valid carriers, invalid duplicate claims, sorted deterministic output.",
      "commit": "feat(evidence): index change carriers",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge index_change_carriers_by_changehash --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 76,
      "step_id": "step_076",
      "phase": "phase_04_carriers_and_evidence",
      "phase_step": 12,
      "title": "Represent invalid evidence without state poisoning",
      "purpose": "Keep diagnostics while excluding invalid objects from candidate graphs.",
      "scope": "Ensure invalid event/carrier is retained by EventId but never inserted as valid control/change candidate.",
      "files": "src/evidence/corpus.rs; indexes.rs",
      "tests": "Invalid control/change sharing pointers/hashes cannot affect candidate sets.",
      "commit": "fix(evidence): isolate invalid carriers from state",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge represent_invalid_evidence_without_state_poisoning --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 77,
      "step_id": "step_077",
      "phase": "phase_04_carriers_and_evidence",
      "phase_step": 13,
      "title": "Represent unsupported revisions",
      "purpose": "Allow future evidence retention without v1 application.",
      "scope": "Store unsupported events separately with declared revision/profile and stable diagnostic; exclude from current graphs/digests except specified disposition digest.",
      "files": "src/evidence/event.rs; src/evidence/corpus.rs",
      "tests": "Unknown revision retained, not applied, deterministic report.",
      "commit": "feat(evidence): retain unsupported revisions safely",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge represent_unsupported_revisions --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 78,
      "step_id": "step_078",
      "phase": "phase_04_carriers_and_evidence",
      "phase_step": 14,
      "title": "Prove acquisition metadata has no semantic path",
      "purpose": "Enforce transport-neutrality in API and tests.",
      "scope": "Keep source/relay/import metadata outside EvidenceCorpus canonical types or in noncanonical diagnostics excluded from evaluator/digests.",
      "files": "src/evidence/source.rs; tests/acquisition_invariance.rs",
      "tests": "Same raw corpus labeled relay/LAN/nearby/import yields identical canonical report.",
      "commit": "test(evidence): prove acquisition invariance",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge prove_acquisition_metadata_has_no_semantic_path --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 79,
      "step_id": "step_079",
      "phase": "phase_04_carriers_and_evidence",
      "phase_step": 15,
      "title": "Handle duplicate carriers correctly",
      "purpose": "Make one valid carrier sufficient for a ChangeHash.",
      "scope": "Define deterministic preferred evidence presentation without making EventId selection affect state; invalid carriers never poison valid.",
      "files": "src/evidence/indexes.rs; src/report.rs",
      "tests": "Valid+invalid permutations and multiple valid authors/events produce one change state.",
      "commit": "feat(evidence): deduplicate state by change hash",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge handle_duplicate_carriers_correctly --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 80,
      "step_id": "step_080",
      "phase": "phase_04_carriers_and_evidence",
      "phase_step": 16,
      "title": "Add carrier/evidence integration fixtures",
      "purpose": "Lock the full ingress-to-corpus layer.",
      "scope": "Add manifest/control/change/unsupported/invalid/duplicate fixture scenarios and expected evidence indexes.",
      "files": "fixtures/v1_draft/carriers/; tests/carrier_evidence.rs",
      "tests": "Run scenarios under seeded event permutations with identical corpus summaries.",
      "commit": "test(evidence): add carrier corpus conformance",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge add_carrier_evidence_integration_fixtures --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 81,
      "step_id": "step_081",
      "phase": "phase_05_control_engine",
      "phase_step": 1,
      "title": "Implement genesis control structural validation",
      "purpose": "Identify valid root governance candidates.",
      "scope": "Validate controller author, coordinate, no parent, sequence 0, empty base_heads, sealed profile, complete grants, terminal consistency.",
      "files": "src/control/validate.rs",
      "tests": "Valid genesis and one negative per invariant.",
      "commit": "feat(control): validate genesis controls",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge implement_genesis_control_structural_validation --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 82,
      "step_id": "step_082",
      "phase": "phase_05_control_engine",
      "phase_step": 2,
      "title": "Implement child parent and sequence validation",
      "purpose": "Build a strict control tree.",
      "scope": "Require exactly one parent tag for non-genesis, existing/pending parent handling, and checked sequence parent+1.",
      "files": "src/control/validate.rs; src/control/tree.rs",
      "tests": "Missing/repeated/wrong parent, gap/overflow, pending-parent tests.",
      "commit": "feat(control): validate parent control sequence",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge implement_child_parent_and_sequence_validation --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 83,
      "step_id": "step_083",
      "phase": "phase_05_control_engine",
      "phase_step": 3,
      "title": "Validate canonical ordering and uniqueness fields",
      "purpose": "Reject repairable-but-signed ACL/frontier variants.",
      "scope": "Require sorted unique device keys, roles, base_heads, continuity references exactly as NIP.",
      "files": "src/control/validate.rs",
      "tests": "Unsorted/duplicate fixtures fail rather than normalize.",
      "commit": "feat(control): enforce canonical control collections",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge validate_canonical_ordering_and_uniqueness_fields --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 84,
      "step_id": "step_084",
      "phase": "phase_05_control_engine",
      "phase_step": 4,
      "title": "Enforce immutable account mapping",
      "purpose": "Prevent an existing device identity from changing owner.",
      "scope": "Compare child grants to parent grants and reject changed account mapping for retained device key.",
      "files": "src/control/transition.rs",
      "tests": "Same mapping valid; changed/null/non-null transitions invalid per spec.",
      "commit": "feat(control): preserve device account identity",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge enforce_immutable_account_mapping --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 85,
      "step_id": "step_085",
      "phase": "phase_05_control_engine",
      "phase_step": 5,
      "title": "Enforce monotonic role reduction",
      "purpose": "Prevent privilege escalation on existing device keys.",
      "scope": "Allow same/subset roles for retained device; reject added role. Require fresh key for elevation.",
      "files": "src/control/transition.rs",
      "tests": "write/checkpoint subset matrix and escalation negatives.",
      "commit": "feat(control): enforce monotonic device roles",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge enforce_monotonic_role_reduction --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 86,
      "step_id": "step_086",
      "phase": "phase_05_control_engine",
      "phase_step": 6,
      "title": "Forbid removed device reintroduction",
      "purpose": "Make revocation monotonic across the entire canonical ancestry.",
      "scope": "Track removed keys along candidate ancestry and reject later reappearance, not only immediate-parent comparison.",
      "files": "src/control/transition.rs; src/control/tree.rs",
      "tests": "Remove then readd after multiple controls; fresh replacement key valid.",
      "commit": "feat(control): prohibit device key reintroduction",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge forbid_removed_device_reintroduction --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 87,
      "step_id": "step_087",
      "phase": "phase_05_control_engine",
      "phase_step": 7,
      "title": "Validate writer and frozen state",
      "purpose": "Define document write liveness from ACL only.",
      "scope": "Compute writer set; support intentional no-writer freeze; ensure manifest status has no effect.",
      "files": "src/control/state.rs",
      "tests": "Writer/checkpointer-only/controller-not-listed/frozen cases.",
      "commit": "feat(control): derive writer and frozen state",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge validate_writer_and_frozen_state --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 88,
      "step_id": "step_088",
      "phase": "phase_05_control_engine",
      "phase_step": 8,
      "title": "Validate terminal and successor continuity",
      "purpose": "Prevent children after terminal and malformed migration references.",
      "scope": "Enforce terminal control child prohibition and exact predecessor/successor field relationships defined by NIP.",
      "files": "src/control/transition.rs",
      "tests": "Terminal child invalid; valid/invalid successor continuity fixtures.",
      "commit": "feat(control): validate terminal successor state",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge validate_terminal_and_successor_continuity --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 89,
      "step_id": "step_089",
      "phase": "phase_05_control_engine",
      "phase_step": 9,
      "title": "Validate base frontier shape",
      "purpose": "Prepare causal epoch transition input.",
      "scope": "Enforce head count limit, sorted uniqueness, nonempty rules for non-genesis where applicable, and ChangeHash syntax already typed.",
      "files": "src/control/validate.rs",
      "tests": "Boundary/duplicate/unsorted/over-limit tests.",
      "commit": "feat(control): validate base frontier",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge validate_base_frontier_shape --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 90,
      "step_id": "step_090",
      "phase": "phase_05_control_engine",
      "phase_step": 10,
      "title": "Define parent accepted-history query interface",
      "purpose": "Decouple control validation from change evaluator without circular mutable state.",
      "scope": "Add internal immutable ParentEpochView exposing accepted hashes, heads, ancestry, actor states, and writer contributions needed by transition validation.",
      "files": "src/control/parent_view.rs; src/reference/internal.rs",
      "tests": "Fake view unit tests; no Automerge mutable type leakage.",
      "commit": "refactor(control): add parent epoch view",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge define_parent_accepted_history_query_interface --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 91,
      "step_id": "step_091",
      "phase": "phase_05_control_engine",
      "phase_step": 11,
      "title": "Enforce retained-writer frontier rule",
      "purpose": "Prevent a controller transition from silently omitting required retained-writer history.",
      "scope": "Implement exact NIP rule over parent accepted history and child base_heads; provide specific diagnostic.",
      "files": "src/control/transition.rs",
      "tests": "One/multiple retained writers, removed writer, excluded branch, missing contribution fixtures.",
      "commit": "feat(control): enforce retained writer frontier",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge enforce_retained_writer_frontier_rule --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 92,
      "step_id": "step_092",
      "phase": "phase_05_control_engine",
      "phase_step": 12,
      "title": "Validate child candidates against parent state",
      "purpose": "Produce otherwise-valid children for deterministic selection.",
      "scope": "Combine structural, transition, base ancestry, profile, and terminal checks into candidate result with pending versus invalid distinction.",
      "files": "src/control/candidate.rs",
      "tests": "Pending missing base change, valid child, invalid transition matrix.",
      "commit": "feat(control): evaluate child control candidates",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge validate_child_candidates_against_parent_state --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 93,
      "step_id": "step_093",
      "phase": "phase_05_control_engine",
      "phase_step": 13,
      "title": "Select canonical child by lowest EventId",
      "purpose": "Guarantee branch convergence without timestamps.",
      "scope": "For each canonical parent select decoded-byte-lowest otherwise-valid child; preserve valid siblings as excluded branch evidence.",
      "files": "src/control/select.rs",
      "tests": "Two/many siblings, arrival permutations, timestamps reversed, exact lexical bytes.",
      "commit": "feat(control): select deterministic control child",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge select_canonical_child_by_lowest_eventid --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 94,
      "step_id": "step_094",
      "phase": "phase_05_control_engine",
      "phase_step": 14,
      "title": "Emit controller equivocation alerts",
      "purpose": "Make governance faults visible.",
      "scope": "Report parent, candidate children, selected child in canonical order whenever >1 otherwise-valid child exists.",
      "files": "src/integrity.rs; src/control/select.rs; src/report.rs",
      "tests": "Alert shape/order and no-alert single-child tests.",
      "commit": "feat(control): report controller equivocation",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge emit_controller_equivocation_alerts --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 95,
      "step_id": "step_095",
      "phase": "phase_05_control_engine",
      "phase_step": 15,
      "title": "Detect and report canonical reorganization",
      "purpose": "Support late lower-ID evidence safely.",
      "scope": "Compare previous optional evaluation summary with new chain or produce reorg details from incremental comparison helper; report old/new tip and affected epochs without changing batch oracle.",
      "files": "src/control/reorganization.rs; src/integrity.rs",
      "tests": "Late lower child changes chain; identical/extension-only cases; deterministic affected IDs.",
      "commit": "feat(control): report canonical control reorganization",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge detect_and_report_canonical_reorganization --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 96,
      "step_id": "step_096",
      "phase": "phase_05_control_engine",
      "phase_step": 16,
      "title": "Add complete control scenario/permutation suite",
      "purpose": "Prove control behavior independent of delivery.",
      "scope": "Add genesis forks, transition matrix, freeze, terminal, retained frontier, missing evidence, and reorganization fixtures under seeded permutations.",
      "files": "fixtures/v1_draft/controls/; tests/control_scenarios.rs",
      "tests": "All complete scenarios produce identical canonical chain/alerts; pending scenarios stable.",
      "commit": "test(control): add deterministic control conformance",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge add_complete_control_scenario_permutation_suite --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 97,
      "step_id": "step_097",
      "phase": "phase_06_change_engine_and_reference_evaluator",
      "phase_step": 1,
      "title": "Create validated change candidate metadata",
      "purpose": "Normalize valid carrier information for graph evaluation.",
      "scope": "Build immutable internal candidate with ChangeHash, actor, seq, start_op, op count, deps, control, author, and valid carrier IDs.",
      "files": "src/graph/change_candidate.rs",
      "tests": "Construction from valid carriers; deterministic valid-carrier set; impossible mismatches diagnosed.",
      "commit": "feat(graph): add validated change candidates",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge create_validated_change_candidate_metadata --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 98,
      "step_id": "step_098",
      "phase": "phase_06_change_engine_and_reference_evaluator",
      "phase_step": 2,
      "title": "Build deterministic dependency graph",
      "purpose": "Represent causal relationships without recursion.",
      "scope": "Create ChangeHash nodes/edges for candidates and accepted base history with BTree ordering and sealed edge/node limits.",
      "files": "src/graph/dependency_graph.rs",
      "tests": "Empty/linear/fork/merge, duplicate deps, self-dep, limit boundaries.",
      "commit": "feat(graph): build change dependency graph",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge build_deterministic_dependency_graph --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 99,
      "step_id": "step_099",
      "phase": "phase_06_change_engine_and_reference_evaluator",
      "phase_step": 3,
      "title": "Implement iterative ancestor closure",
      "purpose": "Calculate exact causal history safely.",
      "scope": "Add stack/queue-based closure with visited set, WorkBudget accounting, missing-dependency result, and canonical output order.",
      "files": "src/graph/closure.rs",
      "tests": "Linear/diamond/deep graph, missing, budget exhaustion, cancellation tests.",
      "commit": "feat(graph): compute bounded ancestor closure",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge implement_iterative_ancestor_closure --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 100,
      "step_id": "step_100",
      "phase": "phase_06_change_engine_and_reference_evaluator",
      "phase_step": 4,
      "title": "Detect cycles and malformed dependencies",
      "purpose": "Fail closed on impossible Automerge graph claims.",
      "scope": "Add iterative cycle detection/topological validation and diagnostics. Distinguish pending missing nodes from invalid cycles/self-dependencies.",
      "files": "src/graph/topology.rs",
      "tests": "Cycle sizes, self-cycle, missing node, deterministic diagnostic ordering.",
      "commit": "feat(graph): reject dependency cycles",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge detect_cycles_and_malformed_dependencies --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 101,
      "step_id": "step_101",
      "phase": "phase_06_change_engine_and_reference_evaluator",
      "phase_step": 5,
      "title": "Initialize actor state from epoch base",
      "purpose": "Validate new changes against exact accepted base closure.",
      "scope": "Derive per-ActorId last sequence/next_op from accepted base changes, detecting pre-existing equivocation/gaps as invalid parent view.",
      "files": "src/graph/actor_state.rs",
      "tests": "Multi-actor base, empty changes, gap/equivocation base negatives.",
      "commit": "feat(graph): derive epoch actor state",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge initialize_actor_state_from_epoch_base --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 102,
      "step_id": "step_102",
      "phase": "phase_06_change_engine_and_reference_evaluator",
      "phase_step": 6,
      "title": "Validate actor predecessor sequence",
      "purpose": "Require per-actor continuity inside accepted dependency closure.",
      "scope": "For seq>1 require exactly one accepted same-actor seq-1 in dependency closure; reject rollback/gap/parallel predecessor.",
      "files": "src/graph/actor_state.rs",
      "tests": "Initial/subsequent, predecessor indirect/direct, missing, two conflicting predecessor tests.",
      "commit": "feat(graph): enforce actor sequence continuity",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge validate_actor_predecessor_sequence --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 103,
      "step_id": "step_103",
      "phase": "phase_06_change_engine_and_reference_evaluator",
      "phase_step": 7,
      "title": "Validate next_op for nonempty changes",
      "purpose": "Prevent operation-ID reuse or gaps.",
      "scope": "Apply checked start_op==next_op and advance by operation count for nonempty accepted candidate.",
      "files": "src/graph/actor_state.rs",
      "tests": "Correct, gap, rollback, overflow, multi-actor tests.",
      "commit": "feat(graph): enforce operation counters",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge validate_next_op_for_nonempty_changes --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 104,
      "step_id": "step_104",
      "phase": "phase_06_change_engine_and_reference_evaluator",
      "phase_step": 8,
      "title": "Validate empty merge change counters",
      "purpose": "Handle Automerge empty changes without underflow.",
      "scope": "Advance sequence, leave next_op unchanged, validate dependencies/frontier, never use upstream max_op.",
      "files": "src/graph/actor_state.rs",
      "tests": "Empty after nonempty, consecutive empties, wrong start_op, sequence and dependency cases.",
      "commit": "feat(graph): handle empty merge changes",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge validate_empty_merge_change_counters --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 105,
      "step_id": "step_105",
      "phase": "phase_06_change_engine_and_reference_evaluator",
      "phase_step": 9,
      "title": "Enforce epoch base ancestry",
      "purpose": "Require every new-epoch change to causally descend from all base heads.",
      "scope": "For candidate referenced control, prove every base head in candidate dependency closure; classify missing evidence pending and omission invalid/excluded per NIP evaluation stage.",
      "files": "src/graph/epoch.rs",
      "tests": "All/some/no base heads, indirect ancestry, missing dep fixtures.",
      "commit": "feat(graph): enforce causal epoch boundary",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge enforce_epoch_base_ancestry --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 106,
      "step_id": "step_106",
      "phase": "phase_06_change_engine_and_reference_evaluator",
      "phase_step": 10,
      "title": "Implement deterministic candidate scheduling",
      "purpose": "Evaluate eligible changes in an order that cannot affect result.",
      "scope": "Use ascending ChangeHash selection among currently dependency-ready candidates; repeat to fixpoint with WorkBudget.",
      "files": "src/graph/schedule.rs",
      "tests": "Arrival/permutation independence, same-ready set lexical order, budget/cancel tests.",
      "commit": "feat(graph): schedule changes deterministically",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge implement_deterministic_candidate_scheduling --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 107,
      "step_id": "step_107",
      "phase": "phase_06_change_engine_and_reference_evaluator",
      "phase_step": 11,
      "title": "Apply changes to exact dependency closure",
      "purpose": "Prove semantic applicability rather than parser validity.",
      "scope": "Build/load Automerge document for exact accepted closure, apply candidate through adapter, verify resulting heads and no hidden extra history.",
      "files": "src/reference/apply.rs; src/automerge_adapter/document.rs",
      "tests": "Valid application, missing closure, incompatible op, extra history negative.",
      "commit": "feat(reference): apply change to exact closure",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge apply_changes_to_exact_dependency_closure --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 108,
      "step_id": "step_108",
      "phase": "phase_06_change_engine_and_reference_evaluator",
      "phase_step": 12,
      "title": "Resolve accepted and pending changes",
      "purpose": "Produce initial epoch dispositions before equivocation.",
      "scope": "Iterate candidate readiness/application, classify accepted/pending/invalid, preserve excluded control-branch candidates separately.",
      "files": "src/reference/epoch.rs",
      "tests": "Dependency arrives late, permanently missing, invalid candidate, duplicate carrier scenarios.",
      "commit": "feat(reference): resolve epoch change dispositions",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge resolve_accepted_and_pending_changes --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 109,
      "step_id": "step_109",
      "phase": "phase_06_change_engine_and_reference_evaluator",
      "phase_step": 13,
      "title": "Detect device equivocation groups",
      "purpose": "Find first actor sequence with multiple distinct otherwise-valid changes.",
      "scope": "Group by ActorId+seq under epoch, identify first conflict, preserve sorted ChangeHashes and valid carrier evidence.",
      "files": "src/graph/equivocation.rs",
      "tests": "No conflict, same hash duplicate, two/many conflicts, later conflict only.",
      "commit": "feat(graph): detect device equivocation",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge detect_device_equivocation_groups --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 110,
      "step_id": "step_110",
      "phase": "phase_06_change_engine_and_reference_evaluator",
      "phase_step": 14,
      "title": "Quarantine equivocation descendants",
      "purpose": "Remove all unsafe history deterministically without a winner.",
      "scope": "Exclude conflicts at first sequence, later same-actor changes, and all transitive dependants; recompute accepted state to fixpoint and emit alert.",
      "files": "src/graph/equivocation.rs; src/reference/epoch.rs; src/integrity.rs",
      "tests": "Cross-actor descendants, independent branches survive, arrival permutations, alert contents.",
      "commit": "feat(graph): quarantine equivocation branches",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge quarantine_equivocation_descendants --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 111,
      "step_id": "step_111",
      "phase": "phase_06_change_engine_and_reference_evaluator",
      "phase_step": 15,
      "title": "Implement full batch reference evaluator",
      "purpose": "Combine control and change engines into the canonical oracle.",
      "scope": "Evaluate from genesis through selected controls/epochs, rebuild when evidence changes selection, materialize final document, and return complete report under budget/cancellation.",
      "files": "src/reference/evaluate.rs; src/reference/mod.rs",
      "tests": "End-to-end basic, concurrent, revocation, fork, equivocation, frozen scenarios.",
      "commit": "feat(reference): add deterministic batch evaluator",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge implement_full_batch_reference_evaluator --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 112,
      "step_id": "step_112",
      "phase": "phase_06_change_engine_and_reference_evaluator",
      "phase_step": 16,
      "title": "Generate canonical report, digests, and typed state assertions",
      "purpose": "Expose cross-language comparison output.",
      "scope": "Implement approved history/disposition digest encoders, canonical ordered report fields, opaque read-only materialization and typed assertion evaluator.",
      "files": "src/report.rs; src/conformance/digest.rs; src/conformance/assertions.rs",
      "tests": "Hand vectors, no save-byte digest, f64/u64/bytes/conflict assertions, permutation-equal report.",
      "commit": "feat(conformance): report canonical document evaluation",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge generate_canonical_report_digests_and_typed_state_ --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 113,
      "step_id": "step_113",
      "phase": "phase_07_conformance_fixtures_and_cli",
      "phase_step": 1,
      "title": "Implement fixture metadata loader",
      "purpose": "Consume neutral fixture definitions safely.",
      "scope": "Parse fixture schema, verify revision, requirement IDs, relative paths, fixed seed, and checksum fields without path traversal.",
      "files": "tools/nostr_automerge_conformance/src/fixture.rs",
      "tests": "Valid fixture; traversal, missing file, duplicate ID, wrong revision negatives.",
      "commit": "feat(conformance): load fixture metadata",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge implement_fixture_metadata_loader --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check",
        "cargo run -p nostr_automerge_conformance -- --help  # replace with the exact step-specific subcommand once implemented"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 114,
      "step_id": "step_114",
      "phase": "phase_07_conformance_fixtures_and_cli",
      "phase_step": 2,
      "title": "Verify fixture file checksums",
      "purpose": "Protect fixture provenance and accidental mutation.",
      "scope": "Recompute SHA-256 for every raw input/expected file and fail before execution on mismatch.",
      "files": "tools/nostr_automerge_conformance/src/checksum.rs",
      "tests": "Valid and modified file tests.",
      "commit": "feat(conformance): verify fixture checksums",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge verify_fixture_file_checksums --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check",
        "cargo run -p nostr_automerge_conformance -- --help  # replace with the exact step-specific subcommand once implemented"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 115,
      "step_id": "step_115",
      "phase": "phase_07_conformance_fixtures_and_cli",
      "phase_step": 3,
      "title": "Parse expected canonical report schema",
      "purpose": "Compare implementation output without ad hoc assertions.",
      "scope": "Load expected report JSON, validate schema, semantic identifiers, canonical ordering, and typed assertions.",
      "files": "tools/nostr_automerge_conformance/src/expected.rs",
      "tests": "Positive/negative schema and unsorted output cases.",
      "commit": "feat(conformance): load expected reports",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge parse_expected_canonical_report_schema --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check",
        "cargo run -p nostr_automerge_conformance -- --help  # replace with the exact step-specific subcommand once implemented"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 116,
      "step_id": "step_116",
      "phase": "phase_07_conformance_fixtures_and_cli",
      "phase_step": 4,
      "title": "Implement canonical report JSON writer",
      "purpose": "Produce stable cross-language bytes.",
      "scope": "Serialize report with exact field order/content rules, lowercase hex, numeric codes, and no nondeterministic maps.",
      "files": "tools/nostr_automerge_conformance/src/report_json.rs",
      "tests": "Golden byte fixture and repeated-run equality.",
      "commit": "feat(conformance): write canonical report JSON",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge implement_canonical_report_json_writer --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check",
        "cargo run -p nostr_automerge_conformance -- --help  # replace with the exact step-specific subcommand once implemented"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 117,
      "step_id": "step_117",
      "phase": "phase_07_conformance_fixtures_and_cli",
      "phase_step": 5,
      "title": "Implement normative history digest encoder",
      "purpose": "Move approved binary digest contract into code.",
      "scope": "Encode exact domain/revision/coordinate/count/ID sequence with checked lengths and SHA-256.",
      "files": "crates/nostr_automerge/src/conformance/history_digest.rs",
      "tests": "Hand-computed examples and ordering negatives.",
      "commit": "feat(conformance): implement history digest",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge implement_normative_history_digest_encoder --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check",
        "cargo run -p nostr_automerge_conformance -- --help  # replace with the exact step-specific subcommand once implemented"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 118,
      "step_id": "step_118",
      "phase": "phase_07_conformance_fixtures_and_cli",
      "phase_step": 6,
      "title": "Implement dispositions digest encoder",
      "purpose": "Bind canonical evidence outcomes.",
      "scope": "Encode item namespaces, identifiers, numeric dispositions, sorted order, counts, and exclude local completion.",
      "files": "src/conformance/dispositions_digest.rs",
      "tests": "Hand examples, local completion invariance, duplicate item rejection.",
      "commit": "feat(conformance): implement dispositions digest",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge implement_dispositions_digest_encoder --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check",
        "cargo run -p nostr_automerge_conformance -- --help  # replace with the exact step-specific subcommand once implemented"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 119,
      "step_id": "step_119",
      "phase": "phase_07_conformance_fixtures_and_cli",
      "phase_step": 7,
      "title": "Implement primitive typed assertions",
      "purpose": "Verify materialized values without lossy JSON.",
      "scope": "Support null, bool, signed/unsigned int, f64 bits, scalar string, bytes, timestamp, counter.",
      "files": "src/conformance/assertions.rs",
      "tests": "One positive/negative per type, u64 max, NaN payload/negative zero if supported by profile.",
      "commit": "feat(conformance): evaluate primitive state assertions",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge implement_primitive_typed_assertions --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check",
        "cargo run -p nostr_automerge_conformance -- --help  # replace with the exact step-specific subcommand once implemented"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 120,
      "step_id": "step_120",
      "phase": "phase_07_conformance_fixtures_and_cli",
      "phase_step": 8,
      "title": "Implement object, text, mark, and conflict assertions",
      "purpose": "Cover structured Automerge semantics.",
      "scope": "Support map/list/table/text object identity/type, text value/indexing, marks, missing paths, and complete conflict alternatives.",
      "files": "src/conformance/assertions.rs",
      "tests": "Nested path, concurrent scalar conflicts, text/emoji/marks, list/table fixtures.",
      "commit": "feat(conformance): evaluate structured state assertions",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge implement_object_text_mark_and_conflict_assertions --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check",
        "cargo run -p nostr_automerge_conformance -- --help  # replace with the exact step-specific subcommand once implemented"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 121,
      "step_id": "step_121",
      "phase": "phase_07_conformance_fixtures_and_cli",
      "phase_step": 9,
      "title": "Add single-fixture CLI command",
      "purpose": "Make one scenario easy to debug.",
      "scope": "Implement `run_fixture <path>` with canonical JSON stdout, diagnostics stderr, stable exit codes, no network.",
      "files": "tools/nostr_automerge_conformance/src/main.rs",
      "tests": "CLI success, expected mismatch, malformed fixture, help tests.",
      "commit": "feat(conformance): run individual fixtures",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge add_single_fixture_cli_command --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check",
        "cargo run -p nostr_automerge_conformance -- --help  # replace with the exact step-specific subcommand once implemented"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 122,
      "step_id": "step_122",
      "phase": "phase_07_conformance_fixtures_and_cli",
      "phase_step": 10,
      "title": "Add corpus CLI command",
      "purpose": "Run all required fixtures and produce a machine report.",
      "scope": "Implement deterministic discovery/order, filters by family/requirement, summary JSON, and nonzero exit on mismatch.",
      "files": "tools/nostr_automerge_conformance/src/main.rs; runner.rs",
      "tests": "Fixture order independence, filter, failure aggregation tests.",
      "commit": "feat(conformance): run fixture corpus",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge add_corpus_cli_command --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check",
        "cargo run -p nostr_automerge_conformance -- --help  # replace with the exact step-specific subcommand once implemented"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 123,
      "step_id": "step_123",
      "phase": "phase_07_conformance_fixtures_and_cli",
      "phase_step": 11,
      "title": "Add seeded permutation runner",
      "purpose": "Exercise event arrival invariance systematically.",
      "scope": "Generate canonical/reverse/seeded permutations from fixture raw events without changing expected canonical report.",
      "files": "tools/nostr_automerge_conformance/src/permutation.rs",
      "tests": "Seed reproducibility and equal complete reports.",
      "commit": "test(conformance): add seeded delivery permutations",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge add_seeded_permutation_runner --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check",
        "cargo run -p nostr_automerge_conformance -- --help  # replace with the exact step-specific subcommand once implemented"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 124,
      "step_id": "step_124",
      "phase": "phase_07_conformance_fixtures_and_cli",
      "phase_step": 12,
      "title": "Add duplicate and delayed-evidence scenario families",
      "purpose": "Cover the failure modes most likely over Nostr.",
      "scope": "Generate duplicate-heavy, dependency-last, control-last, invalid-before-valid-carrier, and late-lower-control variants.",
      "files": "tools/nostr_automerge_conformance/src/scenario_variants.rs",
      "tests": "Expected pending intermediate where specified and equal final canonical report.",
      "commit": "test(conformance): add adversarial delivery variants",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge add_duplicate_and_delayed_evidence_scenario_famili --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check",
        "cargo run -p nostr_automerge_conformance -- --help  # replace with the exact step-specific subcommand once implemented"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 125,
      "step_id": "step_125",
      "phase": "phase_07_conformance_fixtures_and_cli",
      "phase_step": 13,
      "title": "Generate requirement coverage report",
      "purpose": "Show which normative requirements have executable evidence.",
      "scope": "Map fixtures/tests to requirements registry and output missing/covered/unknown references.",
      "files": "tools/nostr_automerge_xtask/src/requirements.rs",
      "tests": "No unknown IDs; intentionally unimplemented checkpoint requirements reported accurately.",
      "commit": "test(spec): report requirement coverage",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge generate_requirement_coverage_report --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check",
        "cargo run -p nostr_automerge_conformance -- --help  # replace with the exact step-specific subcommand once implemented"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 126,
      "step_id": "step_126",
      "phase": "phase_07_conformance_fixtures_and_cli",
      "phase_step": 14,
      "title": "Add repository xtask validation",
      "purpose": "Centralize routine spec/fixture/architecture checks.",
      "scope": "Implement `cargo xtask validate` covering schemas, checksums, snake_case, sealed constants, direct Automerge use, diagnostics, and coverage.",
      "files": "tools/nostr_automerge_xtask/src/validate.rs",
      "tests": "Positive clean repo and unit tests for each validator.",
      "commit": "build(xtask): add repository validation",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge add_repository_xtask_validation --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check",
        "cargo run -p nostr_automerge_conformance -- --help  # replace with the exact step-specific subcommand once implemented"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 127,
      "step_id": "step_127",
      "phase": "phase_07_conformance_fixtures_and_cli",
      "phase_step": 15,
      "title": "Add deterministic conformance CI",
      "purpose": "Prevent nondeterministic output and fixture drift.",
      "scope": "Run corpus twice in clean CI process, compare exact outputs, upload report artifacts, validate checksums and coverage.",
      "files": ".github/workflows/conformance.yml",
      "tests": "Local workflow command/documented equivalent and CI config validation.",
      "commit": "ci(conformance): enforce deterministic fixture results",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge add_deterministic_conformance_ci --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check",
        "cargo run -p nostr_automerge_conformance -- --help  # replace with the exact step-specific subcommand once implemented"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 128,
      "step_id": "step_128",
      "phase": "phase_07_conformance_fixtures_and_cli",
      "phase_step": 16,
      "title": "Publish the Rust core-profile conformance report",
      "purpose": "Create the first auditable implementation evidence.",
      "scope": "Generate report with exact commit, dependencies, fixture manifest, pass/fail, unimplemented checkpoint scope, and checksums.",
      "files": "reports/core_profile_conformance.json; reports/core_profile_conformance.md",
      "tests": "Report schema, checksum, clean checkout reproduction.",
      "commit": "docs(conformance): publish Rust core profile report",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge publish_the_rust_core_profile_conformance_report --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check",
        "cargo run -p nostr_automerge_conformance -- --help  # replace with the exact step-specific subcommand once implemented"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 129,
      "step_id": "step_129",
      "phase": "phase_08_authoring_primitives",
      "phase_step": 1,
      "title": "Define authoring API boundary",
      "purpose": "Add pure authoring only after validation is stable.",
      "scope": "Document and introduce private/public module boundary for deterministic content/change/event drafts; exclude storage, keys, signing services, publication.",
      "files": "src/authoring/mod.rs; docs/api/authoring.md",
      "tests": "Compile/API docs and architecture dependency tests.",
      "commit": "feat(authoring): define pure authoring boundary",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge define_authoring_api_boundary --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 130,
      "step_id": "step_130",
      "phase": "phase_08_authoring_primitives",
      "phase_step": 2,
      "title": "Add explicit ActorState value",
      "purpose": "Make authoring sequence/op transitions durable to callers.",
      "scope": "Define ActorState {actor_id,next_seq,next_op,heads/context as approved} with checked construction and transition result.",
      "files": "src/authoring/actor_state.rs",
      "tests": "Initial, restored, malformed/overflow state tests.",
      "commit": "feat(authoring): add explicit actor state",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge add_explicit_actorstate_value --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 131,
      "step_id": "step_131",
      "phase": "phase_08_authoring_primitives",
      "phase_step": 3,
      "title": "Initialize authoring document deterministically",
      "purpose": "Set profile encoding/actor before any operation.",
      "scope": "Create adapter authoring document from accepted state or empty genesis using explicit UTF-16 and derived ActorId.",
      "files": "src/authoring/document.rs; src/automerge_adapter/document.rs",
      "tests": "No random actor output; exact initial heads/state.",
      "commit": "feat(authoring): initialize deterministic document",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge initialize_authoring_document_deterministically --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 132,
      "step_id": "step_132",
      "phase": "phase_08_authoring_primitives",
      "phase_step": 4,
      "title": "Fix commit metadata",
      "purpose": "Ensure authored change matches v1 profile.",
      "scope": "Set time 0, no message, no extra bytes through approved transaction path; reject caller attempts to vary them.",
      "files": "src/authoring/commit.rs",
      "tests": "Decoded authored changes have exact metadata.",
      "commit": "feat(authoring): enforce canonical commit metadata",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge fix_commit_metadata --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 133,
      "step_id": "step_133",
      "phase": "phase_08_authoring_primitives",
      "phase_step": 5,
      "title": "Create canonical operation-bearing changes",
      "purpose": "Produce one bounded change from caller-authorized local operations.",
      "scope": "Add an internal operation application boundary that commits once, enforces operation/dependency/byte limits, and returns canonical raw change plus transition.",
      "files": "src/authoring/change.rs",
      "tests": "Map/list/text/counter operation examples, empty accidental transaction handling, limits.",
      "commit": "feat(authoring): create canonical Automerge changes",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge create_canonical_operation_bearing_changes --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 134,
      "step_id": "step_134",
      "phase": "phase_08_authoring_primitives",
      "phase_step": 6,
      "title": "Support edit coalescing boundaries",
      "purpose": "Allow applications to commit meaningful batches instead of per-keystroke events.",
      "scope": "Expose builder/session semantics that collect local operations and commit explicitly without timers/network policy in the core.",
      "files": "src/authoring/change_builder.rs",
      "tests": "Multiple operations one change; explicit commit; abort leaves actor state unchanged.",
      "commit": "feat(authoring): support explicit change coalescing",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge support_edit_coalescing_boundaries --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 135,
      "step_id": "step_135",
      "phase": "phase_08_authoring_primitives",
      "phase_step": 7,
      "title": "Create empty fan-in merge changes",
      "purpose": "Consolidate heads for bounded control frontiers.",
      "scope": "Build deterministic sorted bounded dependency fan-in chain, empty change counter behavior, and final head target.",
      "files": "src/authoring/fan_in.rs",
      "tests": "65+ heads, 256 dependency boundary, deterministic output, no op advance.",
      "commit": "feat(authoring): create bounded fan-in changes",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge create_empty_fan_in_merge_changes --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 136,
      "step_id": "step_136",
      "phase": "phase_08_authoring_primitives",
      "phase_step": 8,
      "title": "Build canonical control content",
      "purpose": "Help controllers create spec-correct signed content without hiding governance choices.",
      "scope": "Create typed control draft builder enforcing sorted grants/roles/frontier, sequence/parent, terminal/continuity, and sealed profile. Caller supplies deliberate frontier.",
      "files": "src/authoring/control.rs",
      "tests": "Roundtrip through validator and invalid transition builder errors.",
      "commit": "feat(authoring): build canonical control drafts",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge build_canonical_control_content --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 137,
      "step_id": "step_137",
      "phase": "phase_08_authoring_primitives",
      "phase_step": 9,
      "title": "Build advisory manifest content",
      "purpose": "Create valid discovery hints without making them authoritative.",
      "scope": "Add typed manifest draft builder, sorted relay hints, pointers, application metadata and JCS output.",
      "files": "src/authoring/manifest.rs",
      "tests": "Roundtrip validation; pointer does not change evaluator tests.",
      "commit": "feat(authoring): build canonical manifest drafts",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge build_advisory_manifest_content --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 138,
      "step_id": "step_138",
      "phase": "phase_08_authoring_primitives",
      "phase_step": 10,
      "title": "Create unsigned NIP-01 carrier drafts",
      "purpose": "Separate protocol content from key custody/signing.",
      "scope": "Define internal/public unsigned event draft with kind/tags/content/created_at supplied by caller, canonical NIP-01 preimage/ID preparation.",
      "files": "src/authoring/event_draft.rs",
      "tests": "Exact serialization and event ID preimage fixtures.",
      "commit": "feat(authoring): create unsigned carrier drafts",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge create_unsigned_nip_01_carrier_drafts --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 139,
      "step_id": "step_139",
      "phase": "phase_08_authoring_primitives",
      "phase_step": 11,
      "title": "Add test-only signing roundtrip",
      "purpose": "Prove authored drafts validate through the strict ingress boundary.",
      "scope": "Use deterministic fixture keys in tests only to sign drafts, serialize raw event JSON, ingest and evaluate.",
      "files": "tests/authoring_roundtrip.rs; tests/support/test_signer.rs",
      "tests": "Control/change/manifest roundtrips and wrong signer negatives.",
      "commit": "test(authoring): verify signed draft roundtrip",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge add_test_only_signing_roundtrip --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 140,
      "step_id": "step_140",
      "phase": "phase_08_authoring_primitives",
      "phase_step": 12,
      "title": "Return checked actor-state transitions",
      "purpose": "Prevent caller sequence reuse after successful authoring.",
      "scope": "Return previous/new ActorState and canonical ChangeHash atomically from pure result; errors leave previous state reusable.",
      "files": "src/authoring/change.rs; actor_state.rs",
      "tests": "Success/failure transition, empty/nonempty, overflow tests.",
      "commit": "feat(authoring): expose actor state transitions",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge return_checked_actor_state_transitions --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 141,
      "step_id": "step_141",
      "phase": "phase_08_authoring_primitives",
      "phase_step": 13,
      "title": "Guard against stale/out-of-order actor state",
      "purpose": "Fail closed when caller attempts authoring from stale state.",
      "scope": "Bind ActorState to accepted heads/last authored identity as approved and validate before commit.",
      "files": "src/authoring/actor_state.rs",
      "tests": "Stale duplicate state, changed heads, correct resume tests.",
      "commit": "feat(authoring): reject stale actor state",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge guard_against_stale_out_of_order_actor_state --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 142,
      "step_id": "step_142",
      "phase": "phase_08_authoring_primitives",
      "phase_step": 14,
      "title": "Add authoring negative conformance fixtures",
      "purpose": "Lock builder refusal behavior.",
      "scope": "Add over-limit ops/deps/bytes, wrong metadata, unsorted grants/frontier, invalid successor, stale state fixtures.",
      "files": "fixtures/v1_draft/authoring/; tests/authoring_negative.rs",
      "tests": "Expected stable diagnostics and no state transition.",
      "commit": "test(authoring): add refusal fixtures",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge add_authoring_negative_conformance_fixtures --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 143,
      "step_id": "step_143",
      "phase": "phase_08_authoring_primitives",
      "phase_step": 15,
      "title": "Add pure authoring examples",
      "purpose": "Show correct integration without implying persistence/networking.",
      "scope": "Document derive actor, create control, create change, caller-sign, raw reingest/evaluate. Explicitly mark outbox/signing outside core.",
      "files": "examples/basic_authoring.rs; README.md",
      "tests": "Examples compile and run against fixed in-memory evidence.",
      "commit": "docs(authoring): add pure protocol examples",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge add_pure_authoring_examples --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 144,
      "step_id": "step_144",
      "phase": "phase_08_authoring_primitives",
      "phase_step": 16,
      "title": "Publish authoring API review report",
      "purpose": "Gate downstream Radroots integration on stable enough primitives.",
      "scope": "Review public API, third-party type leakage, semver, state safety, requirements, and tests; record approved gaps.",
      "files": "reports/authoring_api_review.md",
      "tests": "API docs clean; public dependency/type scan; all authoring tests.",
      "commit": "docs(authoring): publish API review",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge publish_authoring_api_review_report --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 145,
      "step_id": "step_145",
      "phase": "phase_09_verified_history_checkpoints",
      "phase_step": 1,
      "title": "Activate checkpoint module and sealed constants",
      "purpose": "Begin the later verified-history checkpoint milestone without changing core validity.",
      "scope": "Add checkpoint module, descriptor/chunk kinds from sealed profile, hash domain constants, and no recovery mode.",
      "files": "src/checkpoint/mod.rs; src/profile.rs",
      "tests": "Constants match spec; forbidden recovery terms validation.",
      "commit": "feat(checkpoint): initialize verified history profile",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge activate_checkpoint_module_and_sealed_constants --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check",
        "cargo run -p nostr_automerge_conformance -- --help  # replace with the exact step-specific subcommand once implemented"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 146,
      "step_id": "step_146",
      "phase": "phase_09_verified_history_checkpoints",
      "phase_step": 2,
      "title": "Parse checkpoint descriptors",
      "purpose": "Represent signed descriptor fields exactly.",
      "scope": "Validate a/e/x tags, controller coordinate, checkpointer author role input, JCS content, encoding, heads, counts, sizes, and draft limits.",
      "files": "src/carrier/checkpoint_descriptor.rs",
      "tests": "Positive descriptor and per-field malformed/unsorted/over-limit fixtures.",
      "commit": "feat(checkpoint): parse checkpoint descriptors",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge parse_checkpoint_descriptors --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check",
        "cargo run -p nostr_automerge_conformance -- --help  # replace with the exact step-specific subcommand once implemented"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 147,
      "step_id": "step_147",
      "phase": "phase_09_verified_history_checkpoints",
      "phase_step": 3,
      "title": "Validate descriptor arithmetic",
      "purpose": "Prevent overflow and inconsistent chunk/count declarations.",
      "scope": "Checked raw_size/chunk_size/chunk_count ceil arithmetic, count/edge/op ranges, nonzero requirements.",
      "files": "src/checkpoint/descriptor.rs",
      "tests": "Boundary, overflow, zero, ceil cases.",
      "commit": "feat(checkpoint): validate descriptor arithmetic",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge validate_descriptor_arithmetic --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check",
        "cargo run -p nostr_automerge_conformance -- --help  # replace with the exact step-specific subcommand once implemented"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 148,
      "step_id": "step_148",
      "phase": "phase_09_verified_history_checkpoints",
      "phase_step": 4,
      "title": "Parse checkpoint chunks",
      "purpose": "Validate exact chunk event/content shape.",
      "scope": "Validate a/e/x/part tags, same author relationship deferred to assembly, strict base64, proof entries, index/count/size constraints.",
      "files": "src/carrier/checkpoint_chunk.rs",
      "tests": "Positive/final/nonfinal chunks and malformed proof/base64/part fixtures.",
      "commit": "feat(checkpoint): parse checkpoint chunks",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge parse_checkpoint_chunks --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check",
        "cargo run -p nostr_automerge_conformance -- --help  # replace with the exact step-specific subcommand once implemented"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 149,
      "step_id": "step_149",
      "phase": "phase_09_verified_history_checkpoints",
      "phase_step": 5,
      "title": "Implement ordered Merkle leaf hashing",
      "purpose": "Match NIP domain-separated chunk leaves.",
      "scope": "Encode index/count/hash exactly with U32BE and calculate leaf; expose internal pure function.",
      "files": "src/checkpoint/merkle.rs",
      "tests": "Hand-computed single/multiple leaf vectors.",
      "commit": "feat(checkpoint): hash ordered Merkle leaves",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge implement_ordered_merkle_leaf_hashing --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check",
        "cargo run -p nostr_automerge_conformance -- --help  # replace with the exact step-specific subcommand once implemented"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 150,
      "step_id": "step_150",
      "phase": "phase_09_verified_history_checkpoints",
      "phase_step": 6,
      "title": "Implement ordered unpadded Merkle root",
      "purpose": "Match the recursive largest-power-of-two split.",
      "scope": "Build root iteratively/recursively only over validated bounded chunk count; document algorithm and deterministic order.",
      "files": "src/checkpoint/merkle.rs",
      "tests": "Counts 1,2,3,5,power-of-two and hand vectors.",
      "commit": "feat(checkpoint): compute ordered Merkle roots",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge implement_ordered_unpadded_merkle_root --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check",
        "cargo run -p nostr_automerge_conformance -- --help  # replace with the exact step-specific subcommand once implemented"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 151,
      "step_id": "step_151",
      "phase": "phase_09_verified_history_checkpoints",
      "phase_step": 7,
      "title": "Verify ordered Merkle proofs",
      "purpose": "Allow independent chunk validation.",
      "scope": "Validate proof length/sides/index path and reconstruct descriptor root.",
      "files": "src/checkpoint/merkle.rs",
      "tests": "Valid proofs for irregular counts; wrong side/hash/length/index negatives.",
      "commit": "feat(checkpoint): verify chunk Merkle proofs",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge verify_ordered_merkle_proofs --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check",
        "cargo run -p nostr_automerge_conformance -- --help  # replace with the exact step-specific subcommand once implemented"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 152,
      "step_id": "step_152",
      "phase": "phase_09_verified_history_checkpoints",
      "phase_step": 8,
      "title": "Assemble chunks with bounded memory",
      "purpose": "Reconstruct exact raw snapshot safely.",
      "scope": "Order by index, require complete unique set/same descriptor+author, validate each chunk, stream/hash/write into bounded buffer or sink abstraction approved for core.",
      "files": "src/checkpoint/assemble.rs",
      "tests": "Out-of-order, duplicate, missing, wrong author/count, budget/cancel, exact/final sizes.",
      "commit": "feat(checkpoint): assemble bounded snapshots",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge assemble_chunks_with_bounded_memory --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check",
        "cargo run -p nostr_automerge_conformance -- --help  # replace with the exact step-specific subcommand once implemented"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 153,
      "step_id": "step_153",
      "phase": "phase_09_verified_history_checkpoints",
      "phase_step": 9,
      "title": "Verify complete snapshot size and hash",
      "purpose": "Bind assembly to descriptor x/raw_size.",
      "scope": "Check total size and SHA-256 before Automerge load.",
      "files": "src/checkpoint/assemble.rs",
      "tests": "Altered chunk, size mismatch, hash mismatch.",
      "commit": "feat(checkpoint): verify snapshot identity",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge verify_complete_snapshot_size_and_hash --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check",
        "cargo run -p nostr_automerge_conformance -- --help  # replace with the exact step-specific subcommand once implemented"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 154,
      "step_id": "step_154",
      "phase": "phase_09_verified_history_checkpoints",
      "phase_step": 10,
      "title": "Load checkpoints with hardened Automerge options",
      "purpose": "Apply the same explicit UTF-16/no-migration/no-partial policy.",
      "scope": "Add checkpoint load adapter, WorkBudget accounting around bytes/change enumeration, and safe errors.",
      "files": "src/automerge_adapter/checkpoint.rs",
      "tests": "Valid save, truncated/invalid/migration attempt, budget/cancel tests.",
      "commit": "feat(checkpoint): load hardened Automerge snapshots",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge load_checkpoints_with_hardened_automerge_options --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check",
        "cargo run -p nostr_automerge_conformance -- --help  # replace with the exact step-specific subcommand once implemented"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 155,
      "step_id": "step_155",
      "phase": "phase_09_verified_history_checkpoints",
      "phase_step": 11,
      "title": "Verify declared checkpoint heads",
      "purpose": "Require exact loaded heads.",
      "scope": "Extract/sort loaded heads and compare to sorted descriptor heads byte-for-byte.",
      "files": "src/checkpoint/verify.rs",
      "tests": "Exact, missing, extra, reordered descriptor negative.",
      "commit": "feat(checkpoint): verify snapshot heads",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge verify_declared_checkpoint_heads --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check",
        "cargo run -p nostr_automerge_conformance -- --help  # replace with the exact step-specific subcommand once implemented"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 156,
      "step_id": "step_156",
      "phase": "phase_09_verified_history_checkpoints",
      "phase_step": 12,
      "title": "Enumerate embedded changes and counts",
      "purpose": "Recompute descriptor commitments from actual snapshot.",
      "scope": "Extract ChangeHashes/deps/ops through adapter, enforce limits, calculate change_count/total_ops/dependency_edges/change_set_hash.",
      "files": "src/checkpoint/verify.rs",
      "tests": "Hand fixture and each count/hash mismatch.",
      "commit": "feat(checkpoint): verify embedded change commitments",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge enumerate_embedded_changes_and_counts --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check",
        "cargo run -p nostr_automerge_conformance -- --help  # replace with the exact step-specific subcommand once implemented"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 157,
      "step_id": "step_157",
      "phase": "phase_09_verified_history_checkpoints",
      "phase_step": 13,
      "title": "Verify exact reachable ancestor closure",
      "purpose": "Reject disconnected extra history.",
      "scope": "Compute closure from heads over embedded deps and require equality with complete embedded change set.",
      "files": "src/checkpoint/verify.rs",
      "tests": "Exact closure, disconnected change, missing dependency, cycle negatives.",
      "commit": "feat(checkpoint): require exact checkpoint closure",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge verify_exact_reachable_ancestor_closure --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check",
        "cargo run -p nostr_automerge_conformance -- --help  # replace with the exact step-specific subcommand once implemented"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 158,
      "step_id": "step_158",
      "phase": "phase_09_verified_history_checkpoints",
      "phase_step": 14,
      "title": "Verify full historical carrier authorization",
      "purpose": "Limit v1 checkpoints to verified history.",
      "scope": "For every embedded hash require valid carrier and accepted status no later than descriptor control; compare control/equivocation/counter history.",
      "files": "src/checkpoint/verify_history.rs",
      "tests": "Missing carrier, excluded branch, later-authorized change, invalid duplicate plus valid carrier.",
      "commit": "feat(checkpoint): enforce verified carrier history",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge verify_full_historical_carrier_authorization --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check",
        "cargo run -p nostr_automerge_conformance -- --help  # replace with the exact step-specific subcommand once implemented"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 159,
      "step_id": "step_159",
      "phase": "phase_09_verified_history_checkpoints",
      "phase_step": 15,
      "title": "Prove checkpoint and full replay agreement",
      "purpose": "Ensure optimization cannot redefine state.",
      "scope": "Evaluate full history and verified checkpoint path, compare controls, accepted set, heads, history digest, typed assertions.",
      "files": "tests/checkpoint_replay_agreement.rs",
      "tests": "Basic/concurrent/revocation/equivocation checkpoint scenarios.",
      "commit": "test(checkpoint): compare checkpoint with full replay",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge prove_checkpoint_and_full_replay_agreement --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check",
        "cargo run -p nostr_automerge_conformance -- --help  # replace with the exact step-specific subcommand once implemented"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 160,
      "step_id": "step_160",
      "phase": "phase_09_verified_history_checkpoints",
      "phase_step": 16,
      "title": "Publish checkpoint conformance fixtures and report",
      "purpose": "Complete full draft checkpoint evidence.",
      "scope": "Add descriptor/chunk/raw snapshots/negative fixtures, permutation transfer order, report and requirement coverage. Exclude any missing-history recovery.",
      "files": "fixtures/v1_draft/checkpoints/; reports/checkpoint_conformance.*",
      "tests": "All checkpoint fixtures pass; forbidden recovery scan; deterministic report.",
      "commit": "test(checkpoint): publish verified history conformance",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge publish_checkpoint_conformance_fixtures_and_report --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check",
        "cargo run -p nostr_automerge_conformance -- --help  # replace with the exact step-specific subcommand once implemented"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 161,
      "step_id": "step_161",
      "phase": "phase_10_hardening_and_alpha_release",
      "phase_step": 1,
      "title": "Initialize cargo-fuzz harness",
      "purpose": "Create reproducible fuzz infrastructure without altering library semantics.",
      "scope": "Add fuzz workspace targets, corpus/seeds, dictionaries, and documented commands.",
      "files": "fuzz/Cargo.toml; fuzz/fuzz_targets/; docs/fuzzing.md",
      "tests": "Build all fuzz targets and run short smoke.",
      "commit": "test(fuzz): initialize fuzz harness",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge initialize_cargo_fuzz_harness --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check",
        "cargo run -p nostr_automerge_conformance -- --help  # replace with the exact step-specific subcommand once implemented"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 162,
      "step_id": "step_162",
      "phase": "phase_10_hardening_and_alpha_release",
      "phase_step": 2,
      "title": "Fuzz strict raw JSON and NIP-01",
      "purpose": "Find parser/signature boundary panics and excessive work.",
      "scope": "Fuzz bounded raw bytes through duplicate scanner, shape parser, serialization and verification adapters with test keys/mocks where appropriate.",
      "files": "fuzz/fuzz_targets/raw_nip01.rs",
      "tests": "No panic; size/work assertions; minimized regression seeds committed.",
      "commit": "test(fuzz): harden raw NIP-01 boundary",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge fuzz_strict_raw_json_and_nip_01 --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check",
        "cargo run -p nostr_automerge_conformance -- --help  # replace with the exact step-specific subcommand once implemented"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 163,
      "step_id": "step_163",
      "phase": "phase_10_hardening_and_alpha_release",
      "phase_step": 3,
      "title": "Fuzz Automerge framing and semantic decode",
      "purpose": "Stress forbidden chunk and upstream boundary.",
      "scope": "Fuzz framing independently and accepted-framing decode/reencode path under limits.",
      "files": "fuzz/fuzz_targets/automerge_framing.rs; automerge_semantics.rs",
      "tests": "No panic/OOM; forbidden decompression unavailable; regressions added.",
      "commit": "test(fuzz): harden Automerge adapter",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge fuzz_automerge_framing_and_semantic_decode --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check",
        "cargo run -p nostr_automerge_conformance -- --help  # replace with the exact step-specific subcommand once implemented"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 164,
      "step_id": "step_164",
      "phase": "phase_10_hardening_and_alpha_release",
      "phase_step": 4,
      "title": "Fuzz control objects and transitions",
      "purpose": "Stress ACL/frontier state machine.",
      "scope": "Generate raw canonical/noncanonical controls and parent chains under small limits.",
      "files": "fuzz/fuzz_targets/control_transition.rs",
      "tests": "No panic; deterministic result on repeated input; regressions.",
      "commit": "test(fuzz): harden control transitions",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge fuzz_control_objects_and_transitions --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check",
        "cargo run -p nostr_automerge_conformance -- --help  # replace with the exact step-specific subcommand once implemented"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 165,
      "step_id": "step_165",
      "phase": "phase_10_hardening_and_alpha_release",
      "phase_step": 5,
      "title": "Fuzz dependency graph and evaluator",
      "purpose": "Stress graph algorithms, equivocation, budgets, and fixpoint.",
      "scope": "Generate bounded corpora/graphs with missing/cycles/duplicates/forks and run evaluator.",
      "files": "fuzz/fuzz_targets/reference_evaluator.rs",
      "tests": "No recursion overflow/panic; budget respected; deterministic output.",
      "commit": "test(fuzz): harden reference evaluator",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge fuzz_dependency_graph_and_evaluator --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check",
        "cargo run -p nostr_automerge_conformance -- --help  # replace with the exact step-specific subcommand once implemented"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 166,
      "step_id": "step_166",
      "phase": "phase_10_hardening_and_alpha_release",
      "phase_step": 6,
      "title": "Fuzz checkpoint parser and Merkle verification",
      "purpose": "Stress later snapshot boundary.",
      "scope": "Fuzz descriptors/chunks/proofs/assembly metadata; feed only bounded validated snapshots to Automerge.",
      "files": "fuzz/fuzz_targets/checkpoint.rs",
      "tests": "No panic/OOM; regressions.",
      "commit": "test(fuzz): harden checkpoint verification",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge fuzz_checkpoint_parser_and_merkle_verification --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check",
        "cargo run -p nostr_automerge_conformance -- --help  # replace with the exact step-specific subcommand once implemented"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 167,
      "step_id": "step_167",
      "phase": "phase_10_hardening_and_alpha_release",
      "phase_step": 7,
      "title": "Expand property-test model",
      "purpose": "Exercise algebraic invariants beyond fixed fixtures.",
      "scope": "Add generated small documents/control trees/change DAGs and invariants for order, idempotence, counters, selection, quarantine and checkpoint agreement.",
      "files": "tests/properties/*.rs",
      "tests": "Seeded reproducibility and configured case counts in CI/nightly.",
      "commit": "test(properties): expand protocol invariants",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge expand_property_test_model --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check",
        "cargo run -p nostr_automerge_conformance -- --help  # replace with the exact step-specific subcommand once implemented"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 168,
      "step_id": "step_168",
      "phase": "phase_10_hardening_and_alpha_release",
      "phase_step": 8,
      "title": "Add resource and performance benchmarks",
      "purpose": "Measure draft limits and regression envelope.",
      "scope": "Benchmark parsing, signatures, graph closure, full replay, duplicates, forks, and checkpoints with metrics/peak memory method.",
      "files": "benches/; tools/nostr_automerge_xtask/src/bench_report.rs",
      "tests": "Stable benchmark smoke and machine-readable report.",
      "commit": "perf(core): add resource benchmarks",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge add_resource_and_performance_benchmarks --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check",
        "cargo run -p nostr_automerge_conformance -- --help  # replace with the exact step-specific subcommand once implemented"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 169,
      "step_id": "step_169",
      "phase": "phase_10_hardening_and_alpha_release",
      "phase_step": 9,
      "title": "Add mutation tests for critical validators",
      "purpose": "Measure whether tests catch security-relevant logic changes.",
      "scope": "Configure mutation testing for framing, NIP-01 ID, control selection, counters, equivocation, digests, Merkle proof.",
      "files": "mutants.toml; docs/mutation_testing.md",
      "tests": "Run selected critical set; record surviving mutants as blockers/issues.",
      "commit": "test(mutation): cover critical protocol validators",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge add_mutation_tests_for_critical_validators --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check",
        "cargo run -p nostr_automerge_conformance -- --help  # replace with the exact step-specific subcommand once implemented"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 170,
      "step_id": "step_170",
      "phase": "phase_10_hardening_and_alpha_release",
      "phase_step": 10,
      "title": "Add coverage reporting",
      "purpose": "Find untested normative branches without treating coverage as correctness.",
      "scope": "Configure coverage command/workflow and requirement-aware summary.",
      "files": ".github/workflows/coverage.yml; docs/coverage.md",
      "tests": "Generate report; no critical parser/state module wholly uncovered.",
      "commit": "ci(coverage): report protocol test coverage",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge add_coverage_reporting --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check",
        "cargo run -p nostr_automerge_conformance -- --help  # replace with the exact step-specific subcommand once implemented"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 171,
      "step_id": "step_171",
      "phase": "phase_10_hardening_and_alpha_release",
      "phase_step": 11,
      "title": "Add dependency, advisory, and license policy",
      "purpose": "Harden supply chain.",
      "scope": "Configure cargo-deny/audit equivalents, allowed licenses/sources, duplicate review, exact Automerge requirement.",
      "files": "deny.toml; .github/workflows/supply_chain.yml",
      "tests": "Policy commands pass or documented approved advisory exceptions.",
      "commit": "build(security): enforce dependency policy",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge add_dependency_advisory_and_license_policy --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check",
        "cargo run -p nostr_automerge_conformance -- --help  # replace with the exact step-specific subcommand once implemented"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 172,
      "step_id": "step_172",
      "phase": "phase_10_hardening_and_alpha_release",
      "phase_step": 12,
      "title": "Generate SBOM and provenance",
      "purpose": "Make releases auditable.",
      "scope": "Add xtask/workflow to produce SPDX/CycloneDX or approved SBOM, dependency/source hashes, fixture manifest, build metadata.",
      "files": "tools/nostr_automerge_xtask/src/sbom.rs; .github/workflows/release.yml",
      "tests": "Deterministic metadata where feasible; artifact schema/checksum.",
      "commit": "build(release): generate SBOM and provenance",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge generate_sbom_and_provenance --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check",
        "cargo run -p nostr_automerge_conformance -- --help  # replace with the exact step-specific subcommand once implemented"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 173,
      "step_id": "step_173",
      "phase": "phase_10_hardening_and_alpha_release",
      "phase_step": 13,
      "title": "Complete public documentation and examples",
      "purpose": "Make the alpha usable without overclaiming.",
      "scope": "Document claim levels, API, strict ingestion, evaluator, authoring, checkpoints, limitations, security, no network/storage. Add runnable examples.",
      "files": "README.md; crates/nostr_automerge/README.md; examples/; docs/",
      "tests": "Docs build; examples compile/run; link check.",
      "commit": "docs(api): complete alpha documentation",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge complete_public_documentation_and_examples --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check",
        "cargo run -p nostr_automerge_conformance -- --help  # replace with the exact step-specific subcommand once implemented"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 174,
      "step_id": "step_174",
      "phase": "phase_10_hardening_and_alpha_release",
      "phase_step": 14,
      "title": "Review public API and semver surface",
      "purpose": "Remove accidental third-party leakage and premature commitments.",
      "scope": "Run public API diff tooling, inspect visibility/types/features, document unstable areas, and make final pre-alpha naming cleanup.",
      "files": "crates/nostr_automerge/src/; reports/api_review.md",
      "tests": "No forbidden public types; semver baseline generated.",
      "commit": "refactor(api): prepare stable alpha surface",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge review_public_api_and_semver_surface --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check",
        "cargo run -p nostr_automerge_conformance -- --help  # replace with the exact step-specific subcommand once implemented"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 175,
      "step_id": "step_175",
      "phase": "phase_10_hardening_and_alpha_release",
      "phase_step": 15,
      "title": "Prepare alpha package and clean-checkout verification",
      "purpose": "Prove the crate can be distributed reproducibly.",
      "scope": "Set alpha version, package metadata, include/exclude files, crates.io dry run, clean checkout build/test/conformance, archive/checksums.",
      "files": "Cargo.toml; CHANGELOG.md; release artifacts",
      "tests": "cargo package dry run; unpacked package tests; full verification suite.",
      "commit": "build(release): prepare nostr_automerge alpha",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge prepare_alpha_package_and_clean_checkout_verificat --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check",
        "cargo run -p nostr_automerge_conformance -- --help  # replace with the exact step-specific subcommand once implemented"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 176,
      "step_id": "step_176",
      "phase": "phase_10_hardening_and_alpha_release",
      "phase_step": 16,
      "title": "Publish security and release readiness report",
      "purpose": "Make claim boundaries and open risks explicit.",
      "scope": "Summarize fuzz/property/mutation/resource/interop status, external review, unresolved issues, approved limits, and release decision. Do not publish if gates fail.",
      "files": "reports/security_readiness.md; reports/release_readiness.json",
      "tests": "Report schema/checksum and sign-off checklist.",
      "commit": "docs(release): publish alpha readiness report",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo test -p nostr_automerge publish_security_and_release_readiness_report --locked  # use the exact test target/filter introduced by the step",
        "cargo run -p nostr_automerge_xtask -- validate  # once the command exists; otherwise run the strongest existing validator",
        "git diff --check",
        "cargo run -p nostr_automerge_conformance -- --help  # replace with the exact step-specific subcommand once implemented"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 177,
      "step_id": "step_177",
      "phase": "phase_11_typescript_interop_and_nip_readiness",
      "phase_step": 1,
      "title": "Publish neutral fixture distribution contract",
      "purpose": "Let independent implementations consume fixtures without Rust tooling.",
      "scope": "Version fixture archive, schemas, checksums, report contract, and download/repository path. No Rust-generated expectations at runtime.",
      "files": "fixtures/DISTRIBUTION.md; reports/fixture_release.json",
      "tests": "Fresh environment validates archive with language-neutral script.",
      "commit": "docs(fixtures): publish neutral conformance corpus",
      "extra": [],
      "verification_commands": [
        "cargo fmt --all --check",
        "cargo check --workspace --all-targets --locked",
        "cargo test --workspace --all-targets --locked",
        "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
        "cargo run -p nostr_automerge_xtask -- validate",
        "# Run the fixture distribution/archive validation command introduced by this step.",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 178,
      "step_id": "step_178",
      "phase": "phase_11_typescript_interop_and_nip_readiness",
      "phase_step": 2,
      "title": "Initialize the independent TypeScript repository",
      "purpose": "Create a genuinely separate protocol implementation.",
      "scope": "In the separate approved repository, add package/toolchain, independent instructions, pinned Automerge JS, copied neutral fixture release, and no Rust dependency.",
      "files": "separate repository: package.json; src/; test/; AGENTS.md",
      "tests": "Install/build/test skeleton; dependency scan proves no nostr_automerge import.",
      "commit": "build(ts): initialize independent interop implementation",
      "extra": [],
      "verification_commands": [
        "# In the independent TypeScript repository, inspect package.json and CI and use the pinned package manager.",
        "pnpm install --frozen-lockfile  # or the repository-approved equivalent",
        "pnpm run format:check          # discover exact script",
        "pnpm run lint                  # discover exact script",
        "pnpm test",
        "pnpm run build",
        "# Run the step-specific fixture or cross-repository differential command.",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 179,
      "step_id": "step_179",
      "phase": "phase_11_typescript_interop_and_nip_readiness",
      "phase_step": 3,
      "title": "Implement TypeScript strict NIP-01 boundary",
      "purpose": "Match raw-event behavior independently.",
      "scope": "Implement duplicate-key raw parsing, canonical NIP-01 serialization, EventId and BIP-340 verification, tags, diagnostics.",
      "files": "TypeScript src/nip01/; test/nip01/",
      "tests": "Same raw NIP-01 fixtures and canonical report diagnostics.",
      "commit": "feat(ts): implement strict NIP-01 validation",
      "extra": [],
      "verification_commands": [
        "# In the independent TypeScript repository, inspect package.json and CI and use the pinned package manager.",
        "pnpm install --frozen-lockfile  # or the repository-approved equivalent",
        "pnpm run format:check          # discover exact script",
        "pnpm run lint                  # discover exact script",
        "pnpm test",
        "pnpm run build",
        "# Run the step-specific fixture or cross-repository differential command.",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 180,
      "step_id": "step_180",
      "phase": "phase_11_typescript_interop_and_nip_readiness",
      "phase_step": 4,
      "title": "Qualify Automerge JS profile",
      "purpose": "Prove JavaScript accepts and produces the same profiled bytes/semantics.",
      "scope": "Implement framing gate independently, explicit text encoding/options where available, semantic decode/reencode qualification, counter metadata and raw fixtures.",
      "files": "TypeScript src/automerge_profile/; test/automerge/",
      "tests": "All Automerge qualification fixtures; mismatch report rather than workaround.",
      "commit": "test(ts): qualify Automerge profile",
      "extra": [],
      "verification_commands": [
        "# In the independent TypeScript repository, inspect package.json and CI and use the pinned package manager.",
        "pnpm install --frozen-lockfile  # or the repository-approved equivalent",
        "pnpm run format:check          # discover exact script",
        "pnpm run lint                  # discover exact script",
        "pnpm test",
        "pnpm run build",
        "# Run the step-specific fixture or cross-repository differential command.",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 181,
      "step_id": "step_181",
      "phase": "phase_11_typescript_interop_and_nip_readiness",
      "phase_step": 5,
      "title": "Implement TypeScript carrier/evidence model",
      "purpose": "Match manifest/control/change parsing independently.",
      "scope": "Implement JCS/base64, sealed revision, carriers, corpus and deterministic indexes from spec/fixtures.",
      "files": "TypeScript src/carrier/; src/evidence/",
      "tests": "Carrier/evidence fixtures and invalid non-poisoning.",
      "commit": "feat(ts): implement carrier evidence corpus",
      "extra": [],
      "verification_commands": [
        "# In the independent TypeScript repository, inspect package.json and CI and use the pinned package manager.",
        "pnpm install --frozen-lockfile  # or the repository-approved equivalent",
        "pnpm run format:check          # discover exact script",
        "pnpm run lint                  # discover exact script",
        "pnpm test",
        "pnpm run build",
        "# Run the step-specific fixture or cross-repository differential command.",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 182,
      "step_id": "step_182",
      "phase": "phase_11_typescript_interop_and_nip_readiness",
      "phase_step": 6,
      "title": "Implement TypeScript control engine",
      "purpose": "Independently validate causal governance.",
      "scope": "Implement transitions, retained frontier, deterministic child selection, alerts and reorganization report.",
      "files": "TypeScript src/control/",
      "tests": "Control scenario/permutation fixtures.",
      "commit": "feat(ts): implement control evaluation",
      "extra": [],
      "verification_commands": [
        "# In the independent TypeScript repository, inspect package.json and CI and use the pinned package manager.",
        "pnpm install --frozen-lockfile  # or the repository-approved equivalent",
        "pnpm run format:check          # discover exact script",
        "pnpm run lint                  # discover exact script",
        "pnpm test",
        "pnpm run build",
        "# Run the step-specific fixture or cross-repository differential command.",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 183,
      "step_id": "step_183",
      "phase": "phase_11_typescript_interop_and_nip_readiness",
      "phase_step": 7,
      "title": "Implement TypeScript change evaluator",
      "purpose": "Independently evaluate authorized Automerge history.",
      "scope": "Implement DAG/closure/counters/epoch/equivocation/application and batch evaluator.",
      "files": "TypeScript src/graph/; src/reference/",
      "tests": "Core change/evaluator fixtures and properties.",
      "commit": "feat(ts): implement change evaluation",
      "extra": [],
      "verification_commands": [
        "# In the independent TypeScript repository, inspect package.json and CI and use the pinned package manager.",
        "pnpm install --frozen-lockfile  # or the repository-approved equivalent",
        "pnpm run format:check          # discover exact script",
        "pnpm run lint                  # discover exact script",
        "pnpm test",
        "pnpm run build",
        "# Run the step-specific fixture or cross-repository differential command.",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 184,
      "step_id": "step_184",
      "phase": "phase_11_typescript_interop_and_nip_readiness",
      "phase_step": 8,
      "title": "Implement TypeScript canonical reports",
      "purpose": "Make cross-language comparison exact.",
      "scope": "Implement history/disposition digests, typed assertions, canonical report serialization and CLI.",
      "files": "TypeScript src/conformance/; bin/",
      "tests": "Hand digest vectors and golden report bytes.",
      "commit": "feat(ts): implement conformance reports",
      "extra": [],
      "verification_commands": [
        "# In the independent TypeScript repository, inspect package.json and CI and use the pinned package manager.",
        "pnpm install --frozen-lockfile  # or the repository-approved equivalent",
        "pnpm run format:check          # discover exact script",
        "pnpm run lint                  # discover exact script",
        "pnpm test",
        "pnpm run build",
        "# Run the step-specific fixture or cross-repository differential command.",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 185,
      "step_id": "step_185",
      "phase": "phase_11_typescript_interop_and_nip_readiness",
      "phase_step": 9,
      "title": "Run and resolve core differential conformance",
      "purpose": "Prove the two protocol implementations agree.",
      "scope": "Run all core fixtures and delivery variants through Rust and TypeScript; classify every mismatch and fix spec/fixture/implementation through change control.",
      "files": "reports/interop_core.*; affected repos/specs",
      "tests": "Zero unexplained mismatches; complete commands/commits/checksums.",
      "commit": "test(interop): achieve core Rust TypeScript agreement",
      "extra": [],
      "verification_commands": [
        "# In the independent TypeScript repository, inspect package.json and CI and use the pinned package manager.",
        "pnpm install --frozen-lockfile  # or the repository-approved equivalent",
        "pnpm run format:check          # discover exact script",
        "pnpm run lint                  # discover exact script",
        "pnpm test",
        "pnpm run build",
        "# Run the step-specific fixture or cross-repository differential command.",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 186,
      "step_id": "step_186",
      "phase": "phase_11_typescript_interop_and_nip_readiness",
      "phase_step": 10,
      "title": "Implement and compare verified-history checkpoints in TypeScript",
      "purpose": "Complete full draft checkpoint interoperability.",
      "scope": "Implement descriptor/chunk/Merkle/closure/history verification and compare with Rust.",
      "files": "TypeScript src/checkpoint/; reports/interop_checkpoint.*",
      "tests": "All checkpoint fixtures and replay-agreement scenarios match.",
      "commit": "test(interop): achieve checkpoint agreement",
      "extra": [],
      "verification_commands": [
        "# In the independent TypeScript repository, inspect package.json and CI and use the pinned package manager.",
        "pnpm install --frozen-lockfile  # or the repository-approved equivalent",
        "pnpm run format:check          # discover exact script",
        "pnpm run lint                  # discover exact script",
        "pnpm test",
        "pnpm run build",
        "# Run the step-specific fixture or cross-repository differential command.",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 187,
      "step_id": "step_187",
      "phase": "phase_11_typescript_interop_and_nip_readiness",
      "phase_step": 11,
      "title": "Run malformed and property differential families",
      "purpose": "Ensure agreement extends beyond happy paths.",
      "scope": "Run malformed raw inputs, seeded generated small graphs, duplicate/order variants, and diagnostic/disposition comparisons.",
      "files": "interop scripts/reports; regression fixtures",
      "tests": "Zero unexplained semantic mismatches; minimized regressions committed.",
      "commit": "test(interop): harden differential edge cases",
      "extra": [],
      "verification_commands": [
        "# In the independent TypeScript repository, inspect package.json and CI and use the pinned package manager.",
        "pnpm install --frozen-lockfile  # or the repository-approved equivalent",
        "pnpm run format:check          # discover exact script",
        "pnpm run lint                  # discover exact script",
        "pnpm test",
        "pnpm run build",
        "# Run the step-specific fixture or cross-repository differential command.",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 188,
      "step_id": "step_188",
      "phase": "phase_11_typescript_interop_and_nip_readiness",
      "phase_step": 12,
      "title": "Establish mismatch triage and ongoing CI",
      "purpose": "Prevent implementations drifting after the first report.",
      "scope": "Add scheduled/cross-repo fixture release CI, mismatch issue template, version pin verification and report artifact retention.",
      "files": "CI in both repos; docs/interop_process.md",
      "tests": "Deliberate mismatch fails CI and produces actionable diff.",
      "commit": "ci(interop): enforce ongoing cross-language agreement",
      "extra": [],
      "verification_commands": [
        "# In the independent TypeScript repository, inspect package.json and CI and use the pinned package manager.",
        "pnpm install --frozen-lockfile  # or the repository-approved equivalent",
        "pnpm run format:check          # discover exact script",
        "pnpm run lint                  # discover exact script",
        "pnpm test",
        "pnpm run build",
        "# Run the step-specific fixture or cross-repository differential command.",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 189,
      "step_id": "step_189",
      "phase": "phase_11_typescript_interop_and_nip_readiness",
      "phase_step": 13,
      "title": "Recheck NIP identifier and event-kind registry",
      "purpose": "Avoid submitting stale/colliding allocations.",
      "scope": "Search current NIPs tree, issues/PRs, and registry of kinds; update preferred NIP-CA recommendation and provisional/final kind decision with evidence.",
      "files": "NIP PR branch; reports/kind_registry_review.md",
      "tests": "Automated collision check plus manual review record.",
      "commit": "docs(nip): refresh identifier and kind allocation",
      "extra": [],
      "verification_commands": [
        "# In the NIPs/spec coordination repository, discover the current Markdown, link, and registry validation commands.",
        "python3 scripts/check_kind_collisions.py  # expected category; use actual repository tool",
        "python3 scripts/validate_spec.py          # expected category; use actual repository tool",
        "# Re-run the latest Rust and TypeScript conformance reports when this step changes a claim.",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 190,
      "step_id": "step_190",
      "phase": "phase_11_typescript_interop_and_nip_readiness",
      "phase_step": 14,
      "title": "Update the draft NIP PR with implementation evidence",
      "purpose": "Move the proposal from design-only draft toward substantive review.",
      "scope": "Update NIP text, prior art, README table guidance, implementation links, fixture release, conformance claims, limits status, and checkpoint packaging decision.",
      "files": "nostr-protocol/nips fork/PR",
      "tests": "Markdown/lint/link checks; NIP references exact immutable releases/commits.",
      "commit": "docs(nip): attach interoperable implementation evidence",
      "extra": [],
      "verification_commands": [
        "# In the NIPs/spec coordination repository, discover the current Markdown, link, and registry validation commands.",
        "python3 scripts/check_kind_collisions.py  # expected category; use actual repository tool",
        "python3 scripts/validate_spec.py          # expected category; use actual repository tool",
        "# Re-run the latest Rust and TypeScript conformance reports when this step changes a claim.",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 191,
      "step_id": "step_191",
      "phase": "phase_11_typescript_interop_and_nip_readiness",
      "phase_step": 15,
      "title": "Publish implementation and security matrix",
      "purpose": "Give maintainers a concise evidence view.",
      "scope": "List Rust/TypeScript features, relay requirements, fixture pass counts, security/resource review, known gaps, and no-overclaim status.",
      "files": "reports/IMPLEMENTATIONS.md; reports/SECURITY_STATUS.md",
      "tests": "Every claim links an artifact/commit; no unsupported production claim.",
      "commit": "docs(nip): publish implementation status",
      "extra": [],
      "verification_commands": [
        "# In the NIPs/spec coordination repository, discover the current Markdown, link, and registry validation commands.",
        "python3 scripts/check_kind_collisions.py  # expected category; use actual repository tool",
        "python3 scripts/validate_spec.py          # expected category; use actual repository tool",
        "# Re-run the latest Rust and TypeScript conformance reports when this step changes a claim.",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    },
    {
      "sequence": 192,
      "step_id": "step_192",
      "phase": "phase_11_typescript_interop_and_nip_readiness",
      "phase_step": 16,
      "title": "Mark NIP ready only after final readiness review",
      "purpose": "Make the ready-for-review transition evidence-based.",
      "scope": "Run complete sign-off checklist, close/block unresolved consensus ambiguities, verify two independent clients and applicable relay compatibility, then update PR status. Otherwise leave draft and publish blockers.",
      "files": "reports/nip_readiness.md; PR metadata",
      "tests": "All gates pass or accurate blocked decision; clean immutable evidence archive.",
      "commit": "docs(nip): complete readiness review",
      "extra": [],
      "verification_commands": [
        "# In the NIPs/spec coordination repository, discover the current Markdown, link, and registry validation commands.",
        "python3 scripts/check_kind_collisions.py  # expected category; use actual repository tool",
        "python3 scripts/validate_spec.py          # expected category; use actual repository tool",
        "# Re-run the latest Rust and TypeScript conformance reports when this step changes a claim.",
        "git diff --check"
      ],
      "expected_result": "Scope complete; required tests and applicable verification pass; repository remains known-good; no unrelated behavior."
    }
  ]
}
